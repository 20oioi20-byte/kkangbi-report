# CHANGELOG.md

> 최신 항목이 위로 오도록 기록합니다. SQL 실행이 필요한 항목은 관련 `schema_addendum_N_*.sql` 파일명을 함께 적습니다.

## 2026-07-19 (14차) — 주요지표 리스트 명칭 정리(2차) + LG전자통합 항목 3개 제거
- **요청 배경**: 대시보드 주요지표 항목 명칭을 센터별로 더 짧고 간결하게 정리해달라는 요청. 전 센터 공통(전월비/전년동월비 표기), KB손보부천, KB손보정비, LG전자통합 각각에 명칭 변경·항목 삭제 지시.
- **전 센터 공통**: "전월비"→"전월比", "전년동월비"→"전년동월比"(한자 比).
- **KB손보부천**(`CENTER_KPI_DEFS.kbsonhae`): 전체재직인원→재직인원, 제휴상담 근태→제휴인원, 제휴 인입호→제휴인입호(공백 제거), SL→제휴SL, 장기손사 근태→손사인원, 손사 인입호→손사인입호(공백 제거), SL→손사SL. 기존에 "제휴상담"/"장기손사" 섹션 접두사로 붙던 방식을 걷어내고 라벨을 그 이름 그대로 지정.
- **KB손보정비**(`CENTER_KPI_DEFS.kbjeongbi`): 전체재직인원→재직인원, 상담사 투입현황→투입인원. 총/고지/통지/목적물 접수건은 변경 없음.
- **LG전자통합**(`CENTER_KPI_DEFS.lge_total`): TO·AS재직인원·성수기재직인원 3개 항목을 리스트에서 완전히 삭제(이 리스트 전용 설정만 삭제되는 것이라 데이터입력 화면·데이터 표·저장 로직에는 전혀 영향 없음 — `LGE_TOTAL_ATT_METRICS` 등 실제 데이터 구조는 그대로). 총재직인원→재직인원, 상담사투입인원→투입인원, 통화시간(IN+OUT)→통화시간으로 변경. T-NPS·생산성(IN+OUT)·생산성(IN)은 그대로 유지.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 각 센터 가짜 데이터로 KB손보부천/KB손보정비/LG전자통합 세 센터의 주요지표 리스트가 요청한 새 명칭 그대로 나오는지, LG전자통합에서 TO/AS재직인원/성수기재직인원 3개 행이 리스트에서 실제로 안 보이는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (13차) — 주요지표 리스트를 원래 한 줄 형태로 복원 + 말줄임(...)·클릭 펼치기 추가
- **요청 배경**: 12차의 정렬 표 레이아웃을 다시 원래(첨부한 스크린샷) 형태 — "라벨 + 일평균/평균/전월비 값을 한 줄로 쭉 이어붙이고, 전월비는 파란 배지로 표시"하는 형태로 되돌려달라는 요청. 추가로, 항목별로 딱 한 줄로만 보이게 하고 내용이 길어서 잘리면 "..."으로 표시하다가, 그 줄을 클릭하면 전체 내용이 다 보이게 해달라는 요청.
- **레이아웃 복원**: `renderTrendList()`를 표 구조에서 다시 원래의 인라인 한 줄 방식으로 되돌림 — 라벨 뒤에 "일평균 값 / 평균 값 / 전월비 [배지] / 전년동월비"를 `&nbsp;/&nbsp;`로 이어붙이고, 전월비는 파란 톤 배지(`.compare-badge`)로, 전년동월비는 배지 없이 텍스트로만 표시(있을 때만 각각 추가) — 12차 이전, 이번 세션에서 레이아웃을 처음 손대기 전의 정확히 그 형태.
- **말줄임 + 클릭 펼치기(신규)**: 각 행(`.trend-row`)에 `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`를 적용해 기본적으로 무조건 한 줄만 보이게 하고, 내용이 넘치면 자동으로 "..."로 잘리도록 함. 행에 `onclick="this.classList.toggle('expanded')"`를 달아 클릭할 때마다 `expanded` 클래스가 토글되고, `.trend-row.expanded`는 `white-space:normal; overflow:visible`이라 잘린 내용 없이 줄바꿈되어 전체가 펼쳐짐(다시 클릭하면 접힘). 12차에서 쓰던 `.trend-tbl`/`.tbl-scroll` 표 스타일은 제거.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 사용자가 첨부한 이미지와 동일한 KB손보정비 데이터로 (1) 화면 텍스트가 이미지와 정확히 일치하는지(라벨/일평균/평균/전월비 배지 순서·문구) (2) 패널을 강제로 좁혀서 실제로 말줄임(`scrollWidth > clientWidth`)이 발생하는지 (3) 클릭 시 `expanded` 클래스가 붙으며 `white-space:normal`로 바뀌어 전체가 보이는지, 다시 클릭하면 원래 상태로 돌아가는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (12차) — 주요지표 리스트를 정렬 표(테이블) 레이아웃으로 재확정
- **요청 배경**: 11차에서 칩 흐름형으로 재설계한 뒤, 평균값을 메인실적 오른쪽에 배치하고 전월비/전년동월비 명칭을 "전월比"/"전년동월比"(한자)로 바꿔달라는 요청이 있었으나, 그 작업 도중 "1안(정렬 표)으로 변경해달라"는 요청으로 전환 — 최종적으로 아티팩트로 제안했던 1안(정렬 표) 그대로 적용.
- **구현**: `renderTrendList()`를 표(table) 구조로 전면 재작성. 지표·일평균·평균·전월比·전년동월比 5개 열을 고정하고, 값이 없는 칸(대개 전년동월비, 작년 같은 달 데이터가 없을 때)은 "–"로 채워 모든 행이 예외 없이 같은 높이가 되도록 함. 헤더의 "일평균"은 기존처럼 기간토글(분기/반기/연초)에 맞춰 "분기평균"등으로 동적 표기. 좁은 화면에서는 `.tbl-scroll`(가로 스크롤 컨테이너)로 표가 잘리지 않게 함. 11차에서 추가했던 `.chip`류 CSS는 `.trend-tbl`/`.tbl-scroll` 표 스타일로 교체.
- **검증**: `node --check` 통과. 로컬 정적 서버 + KB손보부천 데이터로 (1) 표 헤더·행이 의도한 5개 열로 정확히 나오는지 (2) 전월비/전년동월비 데이터가 없는 경우 "–"로 표시되는지(억지로 0이나 빈칸이 되지 않는지) (3) 가로 스크롤 컨테이너가 정상 동작하는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (11차) — 주요지표 리스트를 칩(chip) 레이아웃으로 재설계
- **요청 배경**: 대시보드 "주요지표 (일평균/누적평균)" 리스트가 항목마다 전월비·전년동월비 유무와 글자 길이에 따라 한 줄로 붙거나 갑자기 테두리 박스가 붙어 두 줄이 되는 등 행 높이·정렬이 제각각이라, 깔끔하고 일관되게 정리해달라는 요청. 먼저 UI 시안 3개를 제안하고 시연해달라고 하셔서, 실제 KB손보부천 데이터로 만든 HTML 목업(①정렬 표 ②칩 흐름형 ③고정 2줄형)을 아티팩트로 제시했고, 사용자가 ②칩 흐름형을 선택.
- **구현**: `renderTrendList()`를 전면 재작성. 각 행을 "라벨 + 일평균 값"(1줄) + "평균·전월비·전년동월비를 같은 모양의 칩으로 나열"(둘째 줄) 구조로 변경. 칩은 존재하는 값만 렌더링(평균은 항상, 전월비/전년동월비는 있을 때만)되고 칩 하나의 높이·모양이 항상 동일해서, 칩 개수가 달라져도 "박스가 갑자기 생기는" 느낌 없이 자연스럽게 다음 줄로 흐름. 전월비 칩은 붉은 틴트, 전년동월비 칩은 파란 틴트 배경으로 구분(`style.css`에 `.chip`/`.chip-label`/`.chip.mom`/`.chip.yoy` 신규 추가), 기존 `.compare-stack`/`.compare-badge`/`.compare-yoy`/`.cum-avg`는 더 이상 안 쓰여 제거. 클립보드 복사(`copyTrendList`)용 탭 구분 텍스트 형식은 그대로 유지(화면 표시만 재설계, 복사 데이터 구조는 무변경).
- **검증**: `node --check` 통과. 로컬 정적 서버 + KB손보부천 실제 데이터(스크린샷 기준)로 (1) 전월비·전년동월비가 둘 다 있는 행(전체재직인원, 손사 인입호)과 전월비만 있는 행(제휴 인입호, SL 등)이 모두 칩 스타일로 일관되게 나오는지 (2) 칩의 배경색·테두리·둥근 모양이 의도대로 적용됐는지(computed style로 확인) 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (10차) — KB손보정비 상단 카드 재편 + 공휴일 그래프 숨김 + 센터별 지표 명칭 정리 + 데이터표 접두사 제거
- **요청 배경**: 여러 화면에 걸친 정리 요청 묶음. (1) KB손보정비 상단 지표카드(첨부 이미지1)를 "일평균 접수건·처리건·미처리건" + "월누적 접수건·처리건·미처리건" 구성으로 바꾸고, 상단 단월/분기누적/반기누적/연초누적 토글에 맞춰 값이 반응하게 해달라는 요청. (2) KB손보정비 지표추이 그래프에서 주말은 이미 숨겨지는데 공휴일도 (데이터 삭제 없이 그래프에서만) 숨겨달라는 요청. (3) KB손보정비/KB손보부천/LG전자통합/평택시청의 "주요지표 (일평균/누적평균)" 리스트에서 여러 항목 명칭 변경 요청. (4) 전 센터 공통으로 맨 하단 데이터 표의 "근태·"/"실적·" 접두사 제거 요청.
- **KB손보정비 상단 카드 재편**: `renderKbjeongbiSummaryCards2()`를 "일평균 접수건/처리건/미처리건"(1줄) + "[기간]누적 접수건/처리건/미처리건"(2줄) 구성으로 변경. 기본(단월) 토글에서는 "월누적"이라는 라벨로 이번 달 전체(공휴일·주말 포함) 합계를 보여주고, 분기/반기/연초누적으로 토글하면 라벨·데이터 모두 해당 기간 기준으로 자동 전환(기존에 다른 센터 카드에서 쓰던 `periodCumLabel()`/`cardCumRows` 메커니즘을 그대로 활용). "일평균 미처리건"은 신규로, 날짜별 순증감(접수-처리)을 근무일 기준으로 평균해 계산. 기존에 있던 "처리율"은 이 카드에서 제거(도넛차트로 이미 별도 표시되고 있어 중복).
- **공휴일 그래프 숨김**: KB손보정비 단월 일별 그래프의 주말 필터가 요일만 체크하고 있어서 평일인 공휴일(예: 제헌절 7/17)은 걸러지지 않던 것을, 이미 있던 `isWeekendOrHoliday()`(주말+`KR_HOLIDAYS_2026`)로 교체해서 함께 숨김. 데이터 저장은 그대로, 그래프 표시에서만 제외.
- **지표 명칭 정리**: "누적평균"→"평균"(전 센터 공통), "전체재직인원(TO대비)"→"전체재직인원"(KB손보부천·정비), "총합계/고지의무/통지의무/목적물소멸 접수건"→"총/고지/통지/목적물 접수건"(KB손보정비), "실적·인입호"(제휴상담·장기손사)→"제휴 인입호"/"손사 인입호"(KB손보부천), "TO(합계)" 등 5개 항목에서 "(합계)" 접미사 제거(LG전자통합), "투입인원(TO대비)"→"상담재직인원"(평택시청). 전부 `CENTER_KPI_DEFS`/`renderTrendList()` 설정·텍스트만 바꾼 것이라 계산 로직은 무변경.
- **데이터표 접두사 제거**: `buildMatrix()`(일별 데이터 표)의 열 이름에서 "근태·"/"실적·" 접두사 제거 — 근태/실적 두 그룹이 이미 별도 열 그룹으로 시각적으로 구분돼 있어 접두사가 중복 정보였음. 집계 뷰(`renderAggTable()`)는 원래부터 접두사가 없어서 변경 없음.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터(공휴일 7/17·주말 7/18 포함)로 (1) KB손보정비 카드가 정확히 일평균(근무일만 평균)/월누적(전체 합계, 공휴일·주말 포함) 구성으로 나오고 분기누적 토글 시 라벨·값이 바뀌는지 (2) 그래프에서 7/17·7/18 둘 다 빠지고 근무일만 표시되는지 (3) KB손보정비/KB손보부천/LG전자통합/평택시청 각각의 트렌드 리스트 라벨이 요청한 새 명칭으로 나오는지 (4) 데이터 표 헤더에 "근태·"/"실적·" 접두사가 사라졌는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (9차) — LG전자통합 요약카드 "생산성" 카드 줄 구성 재조정 (T-NPS·통화시간·생산성(IN+OUT) / 생산성(IN)·생산성(OUT))
- **요청 배경**: 8차에서 만든 두 줄 구성(1줄: T-NPS·통화시간 / 2줄: 생산성 3종)을, 생산성(IN+OUT)을 1줄로 옮겨 "1줄: T-NPS·통화시간(IN+OUT)·생산성(IN+OUT) / 2줄: 생산성(IN)·생산성(OUT)"으로 재조정해달라는 요청.
- **구현**: 8차에서 만든 `summaryRows`(줄 단위 2차원 배열) 메커니즘은 그대로 재사용하고, `CENTER_CHART_CONFIG.lge_total.groups[0].summaryRows`의 줄 구성만 변경(생산성(IN+OUT)을 1번째 줄로, 2번째 줄은 생산성(IN)·생산성(OUT) 2항목만). 렌더링 로직(`renderSummaryCards()`)은 줄 개수·줄당 항목 수 모두 가변으로 이미 일반화돼 있어 코드 변경 없이 설정값만 수정.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터로 요약카드가 정확히 "T-NPS/통화시간(IN+OUT)/생산성(IN+OUT)" 1줄(3칸) + "생산성(IN)/생산성(OUT)" 2줄(2칸)로 나오고 값이 정확한지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-19 (8차) — LG전자통합 요약카드 "생산성" 카드를 두 줄(T-NPS·통화시간 / 생산성 3종) 구성으로 변경
- **요청 배경**: 6차에서 한 줄로 배치했던 "생산성" 요약카드를, 1줄엔 T-NPS·통화시간(IN+OUT), 2줄엔 생산성(IN+OUT)·생산성(IN)·생산성(OUT)이 오도록 두 줄로 재배치해달라는 요청.
- **구현**: 6차의 `summaryStats`(한 줄짜리 4항목 목록)와 통화시간 전용이었던 `summaryExtraKeys`를 하나로 통합한 `summaryRows`(줄 단위 2차원 배열)로 교체. `CENTER_CHART_CONFIG.lge_total.groups[0].summaryRows`에 `[[T-NPS, 통화시간(IN+OUT)], [생산성(IN+OUT), 생산성(IN), 생산성(OUT)]]`을 지정하고, `renderSummaryCards()`가 `summaryRows`가 있으면 각 줄을 그대로 `.sc-row`로 렌더링하도록 일반화(줄 개수·줄당 항목 수 모두 가변 지원). 차트(`drawMiniCharts`)는 기존 `barKeys`(생산성 IN/OUT 누적막대)·`lineKeys`(T-NPS 선) 구성을 그대로 쓰므로 영향 없음, 요약카드 레이아웃만 변경.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터(월평균 수동입력값 포함)로 (1) LG전자통합 요약카드가 정확히 "T-NPS/통화시간(IN+OUT)" 1줄 + "생산성(IN+OUT)/생산성(IN)/생산성(OUT)" 2줄로 나오고 값·형식(통화시간은 H:MM:SS)이 정확한지 (2) `summaryRows`를 지정하지 않은 다른 센터(KB손보부천)는 기존 막대/선 레이아웃 그대로 유지되는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-19 (7차) — 주요지표 리스트 전월비/전년동월비 박스를 이미지 기준으로 레이아웃 보강
- **요청 배경**: 5차에서 전월비·전년동월비를 두 줄로 분리했지만, 실제 원하던 모습을 말로 설명하기 어렵다며 이미지를 첨부 — 확인해보니 원했던 건 지금처럼 행 안에서 위→아래로 이어지는 두 줄이 아니라, "왼쪽에는 라벨+일평균/누적평균, 오른쪽에는 테두리로 감싼 박스 안에 전월비(위)·전년동월비(아래)가 두 줄로" 배치되는 좌우 분리 레이아웃이었음.
- **수정**: `renderTrendList()`의 각 행을 `display:flex; justify-content:space-between`로 바꿔 왼쪽(라벨+일평균+누적평균)과 오른쪽(비교 박스)을 좌우로 분리. `style.css`의 `.compare-stack`에 테두리(`border: 1px solid #3a3a3c`, `border-radius: 8px`, `padding`, 우측 정렬) 스타일을 추가해 오른쪽 박스 안에서 전월비·전년동월비가 이미지처럼 위·아래 두 줄로 보이도록 함. 전년동월비가 없을 때(전월비만 있을 때)는 기존처럼 왼쪽 텍스트에 이어서 한 줄로 표시 — 동작 변화 없음.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터로 렌더링된 행의 실제 DOM 구조(`display:flex`, `justify-content:space-between`)와 `.compare-stack`의 계산된 스타일(테두리 있음, 우측 정렬, 자식 2개)이 이미지에서 보이는 좌우 분리 + 테두리 박스 + 두 줄 배치와 일치하는지 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (6차) — LG전자통합 요약카드 "생산성" 첫 줄을 T-NPS/생산성(IN+OUT)/생산성(IN)/생산성(OUT) 순서로 한 줄에 표시
- **요청 배경**: LG전자통합 대시보드의 "생산성" 요약카드 항목 위치를 바꿔서 T-NPS, 생산성(IN+OUT), 생산성(IN), 생산성(OUT) 순서로 한 줄에 보여달라는 요청.
- **구현**: 기존 요약카드는 전 센터가 공유하는 일반 레이아웃(막대 라벨 2개 + 선 라벨 1개, 총 3칸)을 그대로 썼는데, 이 구조로는 4개 항목을 원하는 순서로 한 줄에 배치할 수 없었음. `CENTER_CHART_CONFIG.lge_total.groups[0]`에 신규 옵션 필드 `summaryStats`(표시할 `{key,label}` 목록, 원하는 순서 그대로)를 추가하고, `renderSummaryCards()`가 그룹에 `summaryStats`가 있으면 첫 줄을 이 목록으로 완전히 대체하도록 수정(없으면 기존 막대/선 레이아웃 그대로 — 다른 센터는 영향 없음). 차트(`drawMiniCharts`)는 기존 `barKeys`(생산성 IN/OUT 누적막대)·`lineKeys`(T-NPS 선) 구성을 그대로 쓰므로 그래프 자체 모양은 변경 없음, 요약카드 표시 순서만 바뀜. 생산성(OUT)은 기존처럼 파생값(생산성(IN+OUT)-생산성(IN))을 그대로 사용.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터(월평균 수동입력값 포함)로 (1) LG전자통합 요약카드 첫 줄이 정확히 "T-NPS, 생산성(IN+OUT), 생산성(IN), 생산성(OUT)" 순서·4칸으로 나오고 값도 정확한지(생산성(OUT)=IN+OUT-IN) (2) `summaryStats`를 지정하지 않은 다른 센터(KB손보부천)는 기존 레이아웃 그대로 유지되는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-19 (5차) — 주요지표 리스트의 전월비/전년동월비를 두 줄로 분리 표시
- **요청 배경**: 대시보드 "주요지표 (일평균/누적평균)" 리스트에서 전년동월대비 항목이 나타나는 경우, 전월대비 아래에 위치시켜 전월대비·전년동월대비를 두 줄로 보여달라는 요청.
- **구현**: `renderTrendList()`에서 전월비(`momText`)와 전년동월비(`yoyText`)가 **둘 다 있을 때만** 기존처럼 "일평균 / 누적평균 / 전월비 / 전년동월비"를 한 줄에 이어붙이지 않고, 전월비·전년동월비를 별도 `.compare-stack` 블록으로 분리해 전월비 줄 아래에 전년동월비 줄을 배치. 전년동월비가 없을 때(작년 같은 달 데이터가 없는 경우, 기존에도 흔한 상태)는 기존처럼 전월비만 한 줄에 그대로 표시 — 동작 변화 없음. `CENTER_KPI_DEFS`를 쓰는 모든 센터 공통 함수라 자동으로 전체 적용됨.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터로 (1) 이번달·전월·전년동월 데이터가 모두 있을 때 전월비/전년동월비가 각각 별도 줄(`.compare-stack` 안의 개별 `<div>`)로 렌더링되는지 (2) 전년동월 데이터가 없을 때는 기존처럼 전월비가 한 줄에 인라인으로 표시되는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번 섹션 갱신함.

## 2026-07-19 (4차) — LG전자통합 TO_합계/총재직인원_합계/상담사투입인원_합계를 항상 실시간 재계산해서 표시
- **요청 배경**: 지난 3차 수정(저장 시점에 AS/성수기 한쪽만 입력돼도 합계를 계산·저장하도록 수정)에도 불구하고, 데이터 표의 `근태·TO_합계`/`근태·총재직인원_합계`/`근태·상담사투입인원_합계`가 AS 또는 성수기 값이 없는 경우 여전히 정상적으로 합산되지 않는다는 재신고. `TO_합계 = TO_AS + TO_성수기`, `총재직인원_합계 = 총재직인원_AS + 총재직인원_성수기` 식으로 항상 합산해서 반영해달라는 요청.
- **원인**: 3차 수정은 **저장 시점**(`saveLgeTotalRows`)에만 적용됐다 — 그 수정 이후 새로 저장되는 데이터는 정상이지만, **그 이전에 이미 저장된 과거 데이터**는 여전히 합계 필드가 비어 있거나 부정확한 값으로 남아 있고, 화면은 그 저장된 값을 그대로 읽어서 보여주고 있었기 때문에 문제가 재현됨.
- **수정**: 저장된 값을 신뢰하는 대신, TO_합계/총재직인원_합계/상담사투입인원_합계 3개 필드는 **조회할 때마다 AS+성수기 값을 실시간으로 더해서** 보여주도록 변경(`LGE_TOTAL_LIVE_SUM_KEYS`, `lgeTotalLiveAttSum()`). 공용 저수준 함수 `extractNum()`에 이 3개 필드에 대해서만 실시간 합산으로 가로채는 분기를 추가해서, 이 함수를 거치는 모든 화면(데이터 표의 일별/월별 집계, 요약카드의 "TO 대비 재직인원" 비율, 주요지표 트렌드 리스트, 워크스페이스 전체현황 헤드라인 카드)에 한 번에 정확한 값이 반영되도록 했다. `buildMatrix()`(일별 데이터 표)만 `r.attendance_data[k]`를 직접 읽어 `extractNum()`을 거치지 않는 별도 경로라 동일한 실시간 합산 로직을 추가로 적용. AS·성수기 둘 다 값이 없는 날짜는 여전히 빈칸으로 남기고 억지로 0을 만들지 않음(과거 저장분에 `_합계`로 잘못된 값이 남아있어도 이제 무시되고 항상 재계산됨).
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터(① AS·성수기 둘 다 있지만 저장된 합계 필드는 일부러 틀린 값으로 심어둠 ② AS만 있고 성수기 필드 자체가 없음 ③ 성수기만 있음 ④ 둘 다 없음)로 (1) ①은 저장된 틀린 값이 아니라 재계산된 정확한 합이 나오는지 (2) ②·③은 있는 쪽 값 그대로 합계가 나오는지 (3) ④는 억지로 0이 아니라 빈칸("-")으로 남는지, 일별 표뿐 아니라 월별 집계표(합계/평균)에서도 동일하게 정확한지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-19 (3차) — T-NPS 그래프 주말/공휴일 결측 버그 수정 + LG전자통합 근태합계 부분입력 버그 수정 + KB손보부천 주요지표 항목 정리
- **요청 배경**: (1) LG전자통합 대시보드 그래프에서 T-NPS가 주말과 7월17일·18일에도 반영이 안 되는 이유 확인·해결 요청. (2) 대시보드 하단 데이터 표의 `근태·TO_합계`/`근태·총재직인원_합계`/`근태·상담사투입인원_합계`가 빈칸으로 나오는 경우가 있는데, AS 또는 성수기 중 한쪽만 근태가 입력됐을 때도 합계가 정상 표기되도록 개선 요청. (3) KB손보부천 "주요지표 (일평균/누적평균)"에서 제휴상담/장기손사의 "실적·응대호"·"응답율" 항목을 안 보이게 해달라는 요청.
- **T-NPS 그래프 결측 원인**: `drawMiniCharts()`의 라인 시리즈(`avgSeries`)가 일별(daily) 뷰에서도 `avgExcludingHolidays()`(주말·공휴일 행을 평균 계산에서 제외)를 그대로 썼음. 일별 뷰는 버킷마다 그 날짜 데이터 1건뿐인데, 그 1건이 주말이거나 `KR_HOLIDAYS_2026`에 등록된 공휴일(7/17=제헌절)이면 실제로 값이 입력돼 있어도 필터링되어 평균 계산 대상 자체가 0건이 되면서 무조건 null 처리됨 — 즉 "값이 없어서"가 아니라 "휴일 제외 로직이 실제 입력값까지 지워버려서" 안 보였던 것. LG전자AS처럼 주말에도 실제로 운영하며 값을 입력하는 센터에서 특히 체감되는 버그.
- **수정**: `avgSeries()`가 일별(`aggView === 'daily'`) 뷰에서는 휴일 필터링 없이 그 날짜의 실제 값을 그대로 반환하도록 분리(값이 없으면 null, 있으면 그 값 그대로). 여러 날을 묶는 주/월별 등 집계 뷰는 기존처럼 근무일 기준 평균을 유지(다수 날짜를 평균낼 때 비근무일을 분모에서 빼는 건 여전히 의미가 있어서 그대로 둠). 전 센터 공통 함수라 다른 센터에도 동일하게 적용됨(회귀 없이 정확도만 개선).
- **근태합계 부분입력 버그**: `saveLgeTotalRows()`가 TO/AS재직인원/성수기재직인원/상담사투입인원의 `_합계`를 "AS와 성수기(또는 관리자와 상담사) 둘 다 입력됐을 때만" 계산하고 있었음 — 하루는 AS만 입력하고 성수기는 안 넣었으면(또는 반대) 그 항목의 합계 자체가 저장이 안 돼 데이터 표에서 완전히 빈칸으로 보였고, 이걸 참조하는 파생 항목(`총재직인원_합계`)도 연쇄적으로 비어 보였음. 두 부분 중 하나만 입력돼도 입력 안 된 쪽을 0으로 취급해 합계를 계산·저장하도록 수정.
- **KB손보부천 주요지표 정리**: `CENTER_KPI_DEFS.kbsonhae`에서 제휴상담/장기손사 각각의 "실적·응대호"·"응답율" 항목 제거, "실적·인입호"·"SL"만 남김. 이 리스트 전용 설정이라 요약카드·차트 등 다른 화면에는 영향 없음.
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터로 (1) 7/14~7/19(공휴일 7/17, 주말 7/18~19 포함) 전부에 T-NPS를 채워 넣고 그래프 라인 데이터에 null 없이 전부 반영되는지 (2) AS만 입력하고 성수기는 비워둔 저장 로직을 재현해 TO_합계가 20(AS값)으로 정상 계산되고 둘 다 비었을 땐 합계 자체가 안 생기는지(0을 억지로 만들어내지 않음) (3) KB손보부천 대시보드에서 "실적·응대호"/"응답율"이 안 보이고 "실적·인입호"/"SL"은 그대로 보이는지 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 5번·15번 섹션 갱신함.

## 2026-07-19 (2차) — LG전자통합 대시보드 세부 보정 8건 + 주요지표 목표치 표 전체선택 통일
- **요청 배경**: 지난 대시보드 구성(성수기와 동일 형태로 맞춘 작업) 이후 세부 보정 요청. (1) 지표추이 그래프 툴팁 "총합계"가 T-NPS까지 합산됨 (2) T-NPS 꺾은선이 주말에 끊겨 보임 (3) 막대그래프 주축 범위를 100으로 고정 (4) 핵심지표에 월평균 통화시간 추가(0:00:00 형식) (5) 데이터 표의 TO_합계값 관련 확인 요청 (6) 통화시간_INOUT_초 표를 0:00:00 형식으로 (7) 데이터 표에서 실적 월평균값 컬럼 숨김 (8) 중복된 재직인원 항목(AS재직인원_합계=총재직인원_AS 등) 정리. 별개로 "주요지표 목표치 설정" 표의 "선택" 칸도 TO 설정 표처럼 "전체" 체크박스로 통일 요청.
- **툴팁 총합계 버그**(`drawMiniCharts`): `interaction: {mode:'index'}`라 툴팁이 그 날짜의 모든 데이터셋(막대+선 전부)을 한꺼번에 보여주는데, footer의 합계 계산이 이 전체를 그냥 더하고 있어서 막대(생산성 IN/OUT)뿐 아니라 선(T-NPS)까지 더해지고 있었음. `tooltipItems.filter(ti => ti.dataset.type === 'bar')`로 막대만 합산하도록 수정 — lge_total만이 아니라 stacked+line을 함께 쓰는 모든 센터에 적용되는 일반 버그였어서 전체 공통으로 수정함.
- **T-NPS 라인 끊김**: 라인 데이터셋에 `spanGaps: true` 추가(값이 없는 주말/휴일 지점을 건너뛰고 앞뒤 값을 이어서 그림). 전 센터 라인 데이터셋 공통 적용.
- **막대그래프 주축 범위 100 고정**: `CENTER_CHART_CONFIG` 그룹에 신규 옵션 `barMax` 추가, `lge_total`의 생산성 그룹에만 `barMax: 100` 설정(다른 센터는 미설정 상태라 기존처럼 자동 스케일 그대로).
- **핵심지표에 월평균 통화시간 추가**: 기존 `lineKeys`를 재사용하면 통화시간(수천초 단위)이 차트에도 그려지면서 T-NPS(0~100 범위)와 스케일이 안 맞아 별도 신규 옵션 `group.summaryExtraKeys`/`summaryExtraLabels`를 추가 — 차트에는 그려지지 않고 요약카드에만 `0:00:00` 형식으로 표시됨(`formatSecondsHMS`, 기존에 있던 월평균 override 로직 `avgMetricValue`를 그대로 재사용해 값도 자동으로 월평균 기준).
- **데이터 표 정리**: `collectKeys()`에 `filterVisibleTableKeys()` 필터를 추가해 (a) `_월평균` 접미사 항목 전부 숨김 (b) `AS재직인원_합계`/`성수기재직인원_합계`(항상 `총재직인원_AS`/`총재직인원_성수기`와 동일값)를 숨김. `통화시간_INOUT_초`는 `DURATION_TABLE_KEYS`로 지정해 일별표/집계표(합계·평균) 모두 `0:00:00` 형식으로 표시.
- **TO_합계 관련**: "TO_합계 = 총재직인원_AS+총재직인원_성수기로 적용해달라"는 요청을 코드로 확인한 결과, 그 계산식 자체는 이미 `총재직인원_합계`가 정확히 그렇게 계산되고 있음(`saveLgeTotalRows`에서 확인). `TO_합계`는 목표 재직인원(직접 입력값)으로 실제 재직인원과는 별개 개념이며, 그대로 두지 않으면 요약카드의 "TO 대비 재직인원 비율" 계산(`dynamicTargetAttKey`)이 깨짐 — 용어 혼동으로 판단해 **TO_합계는 변경하지 않음**. 의도가 다르면 다시 확인 요청드립니다.
- **주요지표 목표치 표 전체선택 통일**: "선택" 칸을 TO 설정 표와 동일한 체크박스+"전체" 라벨 방식으로 변경(`kpiGridToggleAll`), 기존 별도 "월 전체 선택"/"월 전체 해제" 버튼은 제거(중복이라 정리).
- **검증**: `node --check` 통과. 로컬 정적 서버 + 가짜 데이터(주말 T-NPS 결측, 중복 재직인원 필드, `_월평균` 필드 포함)로 (1) 툴팁 합계가 T-NPS 제외하고 정확히 계산되는지 (2) 라인 데이터에 null(주말)이 있고 spanGaps가 켜져 있는지 (3) yCount축 max가 100인지 (4) 요약카드에 통화시간이 H:MM:SS로 표시되는지(월평균 저장값 기준) (5) 데이터 표(일별+월별 집계)에서 통화시간이 H:MM:SS로, 월평균/중복 컬럼은 안 보이는지 (6) 주요지표 목표치 표의 "전체" 체크박스가 12개월 전부 토글되는지, 통화시간 목표값 입력칸이 `4:00:00` placeholder를 쓰는지 전부 브라우저에서 직접 확인함.
- `docs/FEATURE.md` 6번·15번 섹션 갱신함.

## 2026-07-19 — LG전자통합 대시보드를 LG전자성수기와 동일한 형태로 구성 + 월평균 실적을 수동입력값 기준으로 표시
- **요청 배경**: (1) LG전자통합의 센터별 대시보드를 LG전자성수기와 동일한 형태(전체 운영요약 카드, 지표추이 미니차트 포함)로 구성해달라는 요청. (2) T-NPS/생산성(IN+OUT)/생산성(IN)/통화시간, 이 4개 실적지표의 "월평균" 표시를 일별 데이터로 자동계산하지 말고 데이터입력 화면에서 수동으로 입력해둔 월평균값(`{key}_월평균`)을 그대로 반영해달라는 요청.
- **원인 파악**: `renderDashboard()`/`renderSummaryCards()` 등 대시보드 렌더링 함수 자체는 이미 완전히 설정객체 기반(config-driven)으로 짜여 있어서 센터별 분기 코드가 거의 없었다 — LG전자통합의 대시보드가 비어 보였던 건 `CENTER_TO_INFO`/`CENTER_CHART_CONFIG`/`DEFAULT_HEADLINE_METRICS`에 `lge_total` 항목 자체가 없어서 관련 함수들이 전부 조용히 빈 값을 반환하고 있었기 때문. 즉 "성수기와 동일한 형태로 구성"은 새 UI를 만드는 게 아니라, 이미 있는 설정객체에 LG전자통합 항목을 채워 넣는 문제였음.
- **대시보드 구성 (LG전자성수기와 동일)**: `CENTER_CHART_CONFIG['lge_total']`(생산성 IN/OUT 누적막대 + T-NPS 선, lge_seongsu와 동일 구성), `CENTER_COMPUTED_METRICS['lge_total']['생산성_OUT_only']`(파생값), `DEFAULT_HEADLINE_METRICS['lge_total']`(워크스페이스 전체현황 카드용) 신규 추가. `CENTER_TO_INFO['lge_total']`도 추가하되, TO가 다른 센터처럼 고정값이 아니라 매일 입력되는 값이라 `dynamicTargetAttKey: 'TO_합계'`로 지정해 그 기간에 입력된 TO 평균 대비로 재직인원 비율을 계산하도록 `renderSummaryCards()`를 일반화(고정 `TO_TARGET`을 쓰는 다른 센터는 동작 변화 없음).
- **월평균 수동입력값 반영**: 신규 공용 함수 `avgMetricValue(key, rows)`를 만들어, `lge_total`의 T-NPS/생산성_INOUT/생산성_IN/통화시간_INOUT_초 4개 지표만 일별 실측 평균(`avgExcludingHolidays`) 대신 그 기간에 걸친 달(들)의 `{key}_월평균` 저장값을 평균해서 반환하도록 분리(`getLgeTotalMonthlyAvgValue`, 여러 달이 걸친 기간이면 각 달의 월평균값을 다시 평균). 파생값인 생산성(OUT)도 일별 실측이 아니라 월평균 기준 INOUT-IN으로 동일하게 계산해 카드에 표시되는 IN+OUT 숫자가 서로 어긋나지 않게 함. `getAvgForDef()`(주요지표 리스트), `renderSummaryCards()`(요약카드), `computeCenterHeadline()`(워크스페이스 전체현황 카드) 세 곳 모두 이 공용 함수를 거치도록 수정 — 다른 센터/다른 지표는 기존 `avgExcludingHolidays` 경로 그대로라 동작 변화 없음. 지표추이 미니차트(일별/주별 등 막대·선 그래프)는 원래대로 일별 실측값을 그대로 보여줌(요청 범위가 "월평균 숫자 표시"였지 차트 자체는 아니었음).
- **검증**: `node --check` 통과. 로컬에서 가짜 데이터(일부러 일별 실적값을 999처럼 말이 안 되는 숫자로 채워두고, 7/1·6/1에만 수동입력 월평균값을 심어둠)로 (1) 요약카드/주요지표 리스트/워크스페이스 헤드라인 카드 전부 999가 아니라 월평균값을 보여주는지 (2) 전월 대비 비교도 월별 월평균값끼리 정확히 비교되는지 (3) 생산성(OUT)이 월평균 IN/INOUT 기준으로 일관되게 계산되는지 (4) TO_합계 대비 % 계산이 맞는지 (5) kbjeongbi 등 다른 센터는 전혀 영향받지 않는지(회귀 확인)까지 전부 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-18 (4차) — KB손보정비 재직및투입현황에 날짜별 "제외" 체크박스 추가
- **요청 배경**: "재직 및 투입현황"에서 근태파일을 추출해 날짜별로 값을 반영할 때, 표 아래(날짜별 행)에서 제외하고 싶은 날짜를 체크하면 그 날짜만 저장에서 빠지는 기능 요청.
- **범위 확인**: 이 "근태파일 추출 → 날짜별 표" 화면은 KB손보정비/LG전자AS/LG전자성수기 3개 센터가 같은 코드(`renderAttRangeTemplate`, `ATTENDANCE_SEMI_AUTO`)를 공유한다. "LG전자통합(`lge_total`)"은 이 화면 자체가 없음(파일 업로드 없이 숫자만 직접 입력하는 별개 화면)을 먼저 안내했고, 사용자가 "LG전자는 제외해달라"고 확인해 **KB손보정비 전용**으로만 추가함.
- **구현**: `renderAttRangeTemplate()`에 `currentCenter === 'kbjeongbi'`일 때만 보이는 "제외" 체크박스 열 추가(`.att-exclude`, LG전자AS/성수기는 기존 표 그대로 변경 없음). 체크하면 그 행이 흐리게 표시됨(기존 `lgeTotalToggleRowExclude()` 재사용 — LG전자통합에 이미 있던 범용 함수라 새로 안 만듦). "근태만 저장"(`saveAttRangeRows`, KB손보정비의 유일한 저장 버튼)이 체크된 날짜를 저장 대상에서 제외하고, 결과 메시지에 "제외" 체크로 뺀 날짜와 투입인원 0명이라 자동 제외된 날짜를 구분해서 표시.
- **검증**: `node --check` 통과. 로컬 정적 서버에서 (1) KB손보정비에만 "제외" 열이 보이고 LG전자AS엔 안 보이는지 (2) 특정 날짜를 제외 체크하고 "근태만 저장"했을 때 그 날짜만 실제 저장 요청에서 빠지고 나머지는 정상 저장되는지 확인함.
- `docs/FEATURE.md` 3번 섹션 갱신함.

## 2026-07-18 (3차) — 🔴 (2차 수정의 진짜 원인) 센터별 대시보드도 같은 이유로 데이터를 못 보고 있었음
- **증상**: 2차에서 "저장된 데이터 조회"는 고쳤는데, 그 데이터가 **센터별 대시보드 현황에는 여전히 안 보인다**는 재신고.
- **원인**: 2차에서 "조회" 기능만 고치면서 "다른 화면(대시보드 등)은 기존 캐시 최적화 그대로 유지"라고 판단했는데, 이게 틀렸다 — **대시보드도 정확히 같은 버그**를 갖고 있었다. 대시보드/데이터입력 등 "특정 센터 화면"은 전부 `loadOverviewForCurrent()`가 채우는 `allRows`를 그대로 쓰는데, 이 함수가 관리자 계정일 때 센터 필터 없이 "전체 센터 통틀어 최근 300건"을 요청하고 있었다(2차와 동일한 원인). 정작 워크스페이스 전체현황(홈)은 이 함수를 아예 안 쓰고 별도의 `loadAllCentersOverview()`를 쓰고 있다는 걸 이번에 다시 확인함 — 즉 "전체 센터 통틀어 최근 300건"으로 요청해야 할 이유가 애초에 `loadOverviewForCurrent()`에는 없었다.
- **수정**: `loadOverviewForCurrent()`가 관리자 여부와 무관하게 **항상** `&center=<현재 센터>`로 한정해서 요청하도록 수정(워크스페이스 전체현황과 완전히 분리된 함수라 안전함 — 전체현황은 `loadAllCentersOverview()`가 그대로 담당). 이제 대시보드·데이터입력·이슈 등 센터 화면 전부가 그 센터의 최근 300건(다른 센터에 밀리지 않음)을 정확히 받아온다.
- **검증**: `node --check` 통과. 로컬에서 관리자(workspaceUnlocked=true)/센터계정(false) 양쪽 모두 `action=admin-overview&center=...`로 정확히 요청되는지, 응답의 오래된 날짜 데이터가 `allRows`에 정상 반영되는지 확인함.
- `docs/FEATURE.md` 2번 섹션 갱신함.

## 2026-07-18 (2차) — 🔴 "저장된 데이터 조회"가 실제로는 저장된 데이터를 0건으로 보여주던 버그 수정
- **증상**: KB손보정비에서 2026-01-14~2026-07-14 데이터를 추출·저장했는데(미리보기 표에서 값 확인됨, "전체 저장" 성공), "저장된 데이터 조회·삭제" 패널에서 2026-01-01~2026-02-28로 조회하면 0건이 나옴. 실제로 저장이 안 된 건지 확인 요청.
- **원인**: "조회" 버튼(`queryDataManage()`)이 서버에 새로 묻지 않고, 화면에 이미 로드돼 있던 전역 변수 `allRows`를 그대로 필터링하고 있었다. 그런데 관리자(도메인 자동인증, `workspaceUnlocked === true`)로 접속하면 `loadOverviewForCurrent()`가 "이 센터"가 아니라 **"전체 센터를 통틀어 최근 300건"**을 `allRows`에 채운다(센터를 전환할 때마다 다시 안 불러오기 위한 캐시 재사용 최적화, `SYSTEM.md`/`CHANGELOG.md` 2026-07-16 "화면 반응속도 개선" 참고). 센터가 5~7개나 되면 다른 센터들의 최근 데이터만으로도 300건이 금방 채워져서, 지금 보고 있는 센터의 반년 전 날짜 데이터는 애초에 `allRows`에 없었던 것 — **실제 DB 저장은 정상이었는데, "조회" 기능 자체가 그 데이터를 볼 수 없는 캐시만 들여다보고 있었던 게 원인**.
- **수정**: `queryDataManage()`가 전역 캐시를 쓰지 않고, "조회"를 누를 때마다 `action=admin-overview&center=<현재 센터>`로 그 센터에 한정해서 서버에 새로 요청하도록 변경. 다른 화면(워크스페이스 전체현황, 센터별 대시보드 등)이 쓰는 `allRows`/`loadOverviewForCurrent()`는 기존 캐시 최적화 그대로 유지(건드리지 않음) — "저장된 데이터 조회" 기능만 독립적으로 항상 정확한 결과를 보도록 격리.
- **검증**: `node --check` 통과. 로컬 정적 서버에서 "다른 센터 최근 데이터만 있는 전역 allRows" + "서버에는 실제로 이 센터의 1~7월 데이터가 있음" 상황을 재현해, 수정 전 방식(allRows 필터)은 0건, 수정 후(서버에 새로 조회)는 정확히 조회되는 것을 직접 확인함.
- **참고**: 실제로 KB손보정비의 1~7월 데이터가 정상 저장돼 있었는지는 이 조회 기능이 고쳐졌으니 다시 조회해서 확인해 주시면 됩니다 — 원래 저장 자체는 실패한 적이 없었을 가능성이 높습니다.
- `docs/FEATURE.md` 2번 섹션에 관련 내용 추가함.

## 2026-07-18 — KB손보정비 실제 파일로 엑셀 버그 재검증·수정 + TO그리드 전체선택
- **요청 배경**: (1) 지난번 다중시트 합산 수정 이후에도 KB손보정비에서 1/14~3/23 데이터가 여전히 안 보인다는 재신고, 사용자가 실제 원본 파일(`KB손보정비센터_0114~0714_데이터.xlsx`)을 직접 첨부해 확인 요청. (2) "TO 및 목표값설정" 화면의 "월별 TO 설정" 표에서도(지난번 "주요지표 목표치" 표에만 추가했던) 월 체크박스 "전체 선택" 기능을 추가해달라는 요청.
- **실제 파일로 검증**(AGENTS.md "추측 금지, 실물 검증 우선" 원칙에 따라 로컬에 `xlsx` 패키지를 설치해 원본 파일을 직접 파싱): 파일은 시트가 1개뿐이라 지난번 "다중 시트" 가설은 이 파일에는 해당하지 않았음. 대신 **데이터 중간(약 8909번째, 10928번째 행)에 헤더 행이 그대로 다시 삽입돼 있는 것을 발견**(여러 조회 결과를 이어붙여 만든 파일로 추정). 이 행을 일반 데이터처럼 처리하면 "요청일"이라는 글자 그대로가 날짜 값으로 들어가고, 문자열 정렬 특성상 모든 "2026-MM-DD" 날짜보다 뒤로 밀리면서 "파일 내 전체 기간" 표시가 `2026-01-14 ~ 요청일`처럼 깨지는 실제 버그를 확인·재현함. `extractLongContractAndFill()`에서 의무위반사항/처리구분 값이 정확히 헤더 텍스트("의무위반사항"/"처리구분")와 일치하는 행은 데이터가 아니라 재삽입된 헤더로 간주해 건너뛰도록 수정.
- **"1/14~3/23만 빠진다"는 원 증상은 이 파일 기준으로는 재현되지 않음**: 헤더 재삽입 버그를 고치기 전에도, 실제 파일을 코드 그대로 시뮬레이션한 결과 1차 추출 단계(`lcBuildDailyRows`)에서는 이미 2026-01-14~2026-07-14 전체가 정상적으로 만들어졌다(172개 날짜, 첫 날짜가 정확히 1/14). 즉 파싱 로직 자체가 앞부분을 자르는 문제는 이 파일에서는 없었음 — 다음에 또 특정 기간만 반영되는 게 보이면, 추출 화면의 "추출 시작일/종료일" 입력칸에 이전에 입력했던 값이 남아있는 상태로(비어있지 않은 채로) 추출하지 않았는지 먼저 확인해 달라고 안내 필요(그 필드에 값이 있으면 상태 메시지에 "· 지정 기간만 반영"이 함께 뜬다).
- **TO 그리드 "전체" 체크박스 추가**(`toGridToggleAll`): "월별 TO 설정" 표의 "선택" 칸을 체크박스가 달린 "전체" 라벨로 바꿔 클릭 한 번으로 12개월 전체 선택/해제 가능(지난번 추가한 "주요지표 목표치" 표의 방식과 동일한 개념).
- **검증**: `node --check` 통과. 실제 파일을 Node에서 직접 파싱해 앱과 동일한 `lc*` 함수 로직으로 재현 테스트(수정 전/후 비교) + 브라우저에서 헤더 재삽입 행이 포함된 가짜 워크북으로 `extractLongContractAndFill()` 실행해 정상 제외되는지, TO그리드 전체선택/해제가 실제로 12개 체크박스에 반영되는지 확인함.
- `docs/FEATURE.md` 2, 6번 섹션 갱신함.

## 2026-07-17 (6차) — 날짜범위 UTC 버그 수정 + AS/성수기 요일 기본반영 + 목표값설정 버그 수정 + KB손보정비 엑셀/차트 수정
- **요청 배경**: (1) LG전자통합 데이터입력에서 7/1~7/31로 기간을 지정하면 6/30이 첫 날짜로 표시되는 버그 신고. (2) "전체반영" 시 AS는 월/화/토/일요일만, 성수기는 평일만 기본으로 채워달라는 요청(실적항목은 예외 없이 전체 반영). (3) 센터별 목표치 설정 화면이 "제대로 작동하지 않는다"는 신고 + 실적항목을 핵심지표로 선택하고 월별 목표치를 등록·전체체크·일괄반영할 수 있게 해달라는 요청 + 목표치 설정된 지표가 전체현황 핵심지표에 자동 포함되게 해달라는 요청. (4) KB손보정비에 1/14~7/14 데이터가 있는 엑셀을 올렸는데 3/24~7/14만 반영된 원인 규명 및 해결. (5) KB손보정비 대시보드 단월 일별 지표추이 그래프에서만 주말 일자를 빼달라는 요청(데이터 자체는 유지).

- **날짜범위 UTC 버그 수정**: `Date.prototype.toISOString()`은 UTC로 변환하는데, UTC보다 시간대가 빠른 한국(UTC+9)에서는 자정 근처 날짜가 하루 앞으로 밀려 보이는 문제가 있었다. 신규 공용 헬퍼 `localDateStr(d)`(로컬 달력 기준 YYYY-MM-DD 생성)를 추가하고, 날짜 범위를 만드는 `lgeTotalDateRange()`(LG전자통합·평택시청 항목별입력이 공유)를 포함해 `new Date().toISOString().slice(0,10)` 패턴이 쓰이던 곳(오늘 날짜 기본값 10곳, `currentMonth` 2곳) 전부를 이 헬퍼로 교체했다.
- **AS/성수기 요일 기본 "전체반영" 규칙**: LG전자통합에서 항목별 "전체반영"을 누르면, AS 계열(TO의 AS, AS재직인원의 관리자/상담사, 상담사투입인원의 AS)은 기본으로 월·화·토·일요일만, 성수기 계열(TO의 성수기, 성수기재직인원의 관리자/상담사, 상담사투입인원의 성수기)은 월~금(평일)만 채워지도록 변경(`lgeTotalColumnGroup`, `lgeTotalAllowedRowIndexes`). 실적 항목(T-NPS/생산성/통화시간)은 요일과 무관하게 항상 표에 보이는 전체 날짜에 적용된다. 규칙에서 빠진 날짜도 표에서 직접 입력하는 건 항상 가능(자동채움 대상만 좁히는 것).
- **목표값설정 화면 버그 수정**:
  - **핵심 버그**: "지표 선택" 드롭다운(`buildMetricOptionsForCenter`)이 전역 `rowSchema`를 참조하고 있었는데, `rowSchema`는 "데이터입력 화면에서 마지막으로 연 센터"의 스키마로만 채워지는 변수다. 즉 설정화면을 다른 센터에서 바로 열거나, 아예 데이터입력 화면을 안 거치고 설정화면부터 열면 드롭다운이 비어있거나 엉뚱한 센터의 항목을 보여줬다(LG전자통합은 애초에 이 전역변수를 채우지도 않아 항상 비어 있었음). `settingsMetricSchemaCache` + `loadSettingsMetricSchema()`를 신규 추가해 설정화면을 열 때마다 그 센터의 실적항목을 새로 조회하도록 수정(LG전자통합은 서버 스키마가 없어 `LGE_TOTAL_PERF_METRICS`를 고정 목록으로 사용). 이게 사실상 "센터별로 데이터에 있는 실적항목을 핵심지표로 선택할 수 있게 해달라"는 요청의 실제 원인이었다 — 기능 자체는 이미 있었지만 드롭다운이 고장나 있어 못 썼다.
  - **"월 전체 선택/해제" 추가**(`kpiGridToggleAll`): 월별 목표치 표의 체크박스를 한 번에 전부 체크/해제할 수 있는 버튼 추가.
  - **"일괄 반영"이 저장까지 하도록 수정**(`applyKpiGridBulk`): 예전엔 체크된 월의 입력칸에 값만 채우고, 별도로 "월별 목표치 전체 저장" 버튼을 또 눌러야 실제로 DB에 저장됐다. 이 두 번째 클릭을 잊으면 화면 이동/새로고침 시 입력값이 통째로 사라져 "반영이 안 된다"는 문제로 이어졌다 — 이제 일괄 반영을 누르면 곧바로 `saveKpiGrid()`까지 호출해서 저장까지 한 번에 끝난다.
  - **"목표치 설정된 지표가 전체현황 핵심지표에 포함"은 이미 정상 동작 중이었음을 확인**: `computeCenterHeadline()`이 `kpiSettingsCache[code]`에 등록된 지표가 있으면 그걸(전체, sort_order 순) 핵심지표로 쓰고 없으면 센터별 기본 지표세트로 fallback하는 로직이 이미 있었다 — 위 드롭다운 버그 때문에 지표를 애초에 등록할 수 없어서 이 기능이 있는 줄도 모르고 있었던 것으로 보임. 별도 구현 불필요, 테스트로 정상 동작 재확인만 함.
- **KB손보정비 엑셀 자동추출: 여러 시트 합산 안 되던 버그 수정**(`extractLongContractAndFill`): 필수 컬럼(처리구분/의무위반사항/요청일/완료일/변경기한일)을 가진 시트가 워크북에 여러 개 있어도, 예전엔 **가장 먼저 발견된 시트 하나만** 읽고 나머지는 전부 버렸다(`break`로 루프 중단). 이게 "1/14~7/14 데이터 중 3/24~7/14만 반영된" 원인으로 추정된다 — 파일이 월별 등 여러 시트로 나뉘어 있었다면, 처음 조건을 만족한 시트만 읽히고 나머지 시트 데이터는 통째로 무시됐을 것이다. 이제 조건을 만족하는 시트를 전부 찾아 각각 파싱한 뒤 레코드를 합치도록 수정, 추출 완료 메시지에 몇 개 시트를 합쳤는지 표시. ⚠️ 실제 원본 파일로 재현 확인은 못 했음(AGENTS.md 원칙상 실물 검증이 이상적이나 파일이 없어 코드 분석 기반으로 가장 유력한 원인을 수정) — 다음 업로드 때 전체 기간이 반영되는지 확인 필요.
- **KB손보정비 대시보드: 단월 일별 지표추이 그래프에서만 주말 제외**(`drawMiniCharts`): `aggView === 'daily'`이고 `currentCenter === 'kbjeongbi'`일 때만 토·일요일 데이터를 그래프 버킷 구성에서 제외하도록 필터 추가. 원본 데이터(`allRows`/저장된 실적)와 다른 기간 토글(분기/반기/누적)·다른 센터의 그래프는 전혀 영향 없음.
- **검증**: `node --check` 통과. 로컬 정적 서버에서 (1) 7/1~7/31 지정 시 정확히 31개 날짜(7/1~7/31)가 생성되는지 (2) AS/성수기 전체반영이 지정된 요일에만 적용되고 실적항목은 전체 적용되는지 (3) 센터를 바꿔가며 설정화면을 열었을 때 지표 드롭다운이 그 센터 것만 정확히 보여주는지(LG전자통합 포함) (4) "월 전체 선택" 후 "일괄 반영"이 실제 저장 요청까지 보내는지 (5) `computeCenterHeadline`이 등록된 지표를 핵심지표로 쓰는지 (6) 가짜 2개 시트(1월/7월) 워크북으로 두 시트 모두 합산되는지까지 전부 확인함. 실제 KB손보정비 엑셀 파일로는 재검증 필요.
- `docs/FEATURE.md` 2, 6번 섹션 갱신함.

## 2026-07-17 (5차) — LG전자통합 UI 단순화(자동계산 열 숨김) + 월평균 "대상 월" 직접선택 + 통화시간 자동 포맷
- **요청 배경**: (1) 월평균 입력을 "시작일이 속한 달"로 자동 결정하지 말고 "YYYY년 MM월"을 직접 선택하게 해달라는 요청. (2) 표에서 자동으로 계산되는 값(총재직인원, 각 항목의 합계)은 화면에 보여줄 필요 없이 저장할 때만 자동 반영되면 되고, 입력하는 화면에는 안 보이게 해달라는 요청. (3) 통화시간 입력 시 숫자만(예: `40000`) 입력해도 `4:00:00` 형식으로 자동 반영해달라는 요청. (4) 이후로는 커밋/푸시 등 승인 여부를 매번 묻지 말고 자동으로 진행해달라는 요청.
- **월평균 "대상 월" 직접 선택**: 월평균 입력 패널 맨 위에 `<input type="month">` 선택자(`lgeTotalAvgMonth`)를 추가. 이제 월평균은 "시작일"과 완전히 무관하게, 이 선택자로 고른 달의 1일 데이터에 저장된다(`lgeTotalAvgMonthDate()`). 선택자를 바꾸거나 화면을 처음 열 때 그 달에 이미 저장된 값이 있으면 자동으로 불러옴(`refreshLgeTotalMonthlyAvgInputs()`). 기존에 "기간 적용" 버튼을 눌러야만 월평균이 채워지던 로직은 제거하고 독립적으로 동작하도록 분리.
- **자동계산 열 화면에서 숨김**: 총재직인원(AS/성수기/합계 전부)과 TO·AS재직인원·성수기재직인원·상담사투입인원 각각의 "합계" 열을 표에서 완전히 제거(`LGE_TOTAL_VISIBLE_ATT_METRICS`로 화면에 보일 항목만 필터링). 사용자가 직접 입력하는 세부값만 보이고, 합계·총재직인원은 `saveLgeTotalRows()`가 저장 직전에 값들을 읽어 계산해서 payload에 채워 넣는다. 실시간 화면 재계산이 필요 없어져 `lgeTotalRecalc()` 함수와 관련 `oninput` 핸들러를 전부 제거(구조가 단순해짐). 일괄입력 행/전체반영 버튼도 화면에 보이는 항목만 대상으로 축소.
- **통화시간 자동 포맷**: `formatDurationOnBlur()` 신규 추가 — 통화시간 입력칸(일자별 실적, 일괄입력 행, 월평균 입력, 단일 날짜 수정 화면 전부)에 숫자만 입력한 뒤 포커스를 벗어나면 오른쪽부터 초/분/시 순으로 나눠 `H:MM:SS`로 자동 변환(예: `40000`→`4:00:00`, `145`→`0:01:45`). 이미 콜론이 있는 값은 그대로 둠.
- **이후 작업 방식 변경**: 사용자 요청에 따라 앞으로 이 프로젝트에서는 git 커밋/푸시를 진행 전 승인 요청 없이 바로 실행한다(단, Supabase SQL 실행이나 Vercel 배포처럼 채팅에서 직접 실행할 수 없는 작업은 여전히 사용자가 별도로 진행해야 함).
- **검증**: `node --check` 통과. 로컬 정적 서버에서 가짜 이력 데이터로 (1) 대상 월 선택자가 시작일과 무관하게 동작하는지 (2) 총재직인원/합계 열이 화면에 전혀 안 보이는지 (3) 저장 payload에는 그 값들이 정확히 계산되어 포함되는지 (4) 통화시간 `40000`→`4:00:00` 변환이 blur 이벤트로 정상 동작하는지까지 전부 확인함.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-17 (4차) — 평택시청 항목별 직접입력 + KB손보정비 실적/근태 분리 + 목적물소멸 판정 수정
- **요청 배경**: (1) 평택시청의 "직접 붙여넣기"/"업무유형별 현황"에도 방금 만든 LG전자통합 항목별 입력과 동일한 형태의 입력 방식을 텍스트 붙여넣기는 유지한 채 추가해달라는 요청. (2) KB손보정비는 엑셀추출/붙여넣기 → 재직및투입현황으로 자동 연계되어 실적+근태를 한꺼번에 저장하는 구조였는데, 실적과 근태 저장을 완전히 분리하고 순서와 무관하게 서로의 값이 사라지지 않게 해달라는 요청. (3) KB손보정비 엑셀에서 "목적물소멸" 건수를 셀 때 단독 값만이 아니라 다른 사유와 콤마로 같이 적힌 값도 전부 포함해달라는 요청.
- **평택시청 항목별 직접입력 추가**: 기존 텍스트 붙여넣기 UI(`pasteHtml`, `renderCategoryEntryPanel`)는 그대로 두고 그 아래에 새 패널을 추가. `renderPtDailyItemShell()`/`generatePtDailyItemRows()`(일자별 실적, `rowSchema` 기반)와 `renderPtCategoryItemShell()`/`generatePtCategoryItemRows()`(업무유형별 현황, `categorySchema` 기반) — 둘 다 LG전자통합과 동일하게 날짜범위 지정 → 일괄입력 행 + 항목별/전체 "전체반영" + 날짜별 "제외" 체크박스 구조. 날짜 범위 생성(`lgeTotalDateRange`)과 제외 체크박스 시각효과(`lgeTotalToggleRowExclude`)는 LG전자통합 코드를 그대로 재사용해 중복을 줄임. 일자별 실적 쪽은 근태 필드(투입인원)만 직전 저장일 값으로 이월, 나머지는 매일 새로 입력. 업무유형별 쪽은 저장 시 "합계" 대비 비중(%)을 자동 계산해 함께 저장(기존 `saveCategoryRows`와 동일 계산식). 저장은 둘 다 `manual-entry-bulk` 재사용(신규 백엔드 액션 없음).
- **KB손보정비 실적/근태 완전 분리**: `promptGoToAttendance()`가 KB손보정비일 때는 더 이상 확인창을 띄우고 자동으로 "재직 및 투입현황" 탭으로 이동시키지 않도록 수정(기간만 `pendingAttRange`에 기억해뒀다가 나중에 그 탭을 열면 자동으로 채워짐). "재직 및 투입현황" 화면은 KB손보정비에 한해 `generateAttRangeRows()`에서 "근태만 저장" 버튼만 노출하고 "실적+근태 일괄 반영"/"실적만 저장" 버튼은 숨김. 실적은 반드시 "일자별 실적 직접입력" 화면의 "전체 저장"으로만 저장됨. 두 저장 함수(`saveAllRows()`/`saveAttRangeRows()`) 모두 자기 그룹 필드만 전송하는 기존 구조 그대로라, 실적을 먼저 저장하든 근태를 먼저 저장하든 서로의 값을 지우지 않는다(기존에 검증된 `manual-entry-bulk` 부분 필드 병합 동작에 근거 — 별도 백엔드 확인 없이 진행). LG전자AS/성수기는 영향 없음.
- **목적물소멸 판정 방식 변경** (`lcClassify()`): 기존엔 의무위반사항이 정확히 "단순변경"이면서 처리구분에 "목적물소멸"이 포함된 행만 인정했음. 이제는 의무위반사항/처리구분 어느 값에든 "목적물소멸"이라는 단어가 포함되면 무조건 목적물소멸 1건으로 집계(예: "목적물소멸,담보소멸", "담보납입면제,목적물소멸,담보무효취소" 전부 포함). **AGENTS.md "추측 금지, 실물 검증 우선" 원칙에 대한 예외**: 사용자가 준 값 예시가 의무위반사항/처리구분 중 정확히 어느 컬럼의 값인지 실제 엑셀 파일 없이는 확인할 수 없어서, 두 컬럼 모두에 안전하게(기존에 맞게 분류되던 것들은 그대로 유지되는 상위집합 방식으로) 적용했다 — 실제 파일로 재검증 필요.
- **검증**: `node --check` 통과. 평택시청 두 표 모두 로컬 정적 서버에서 가짜 rowSchema/categorySchema/이력 데이터로 (1) 스키마 기반 헤더 생성 (2) 근태 필드만 이월되고 나머지는 비어있는지 (3) 일괄입력+전체반영 (4) 제외 체크박스가 실제 저장 payload에서 빠지는지 (5) 카테고리 비중(%) 계산까지 전부 확인함. KB손보정비 분리/목적물소멸 판정은 실제 파일·저장 데이터로 재검증 필요.
- `docs/FEATURE.md` 2·3번 섹션 갱신함.

## 2026-07-17 (3차) — LG전자통합 항목 구조 개편 + 월평균 입력 + 날짜별 제외 체크박스
- **요청 배경**: (1) 재직인원 항목을 "역할(관리자/상담사) 축으로 AS·성수기를 나눠 입력"하던 것을, "그룹(AS/성수기) 축으로 관리자·상담사를 나눠 입력"하는 방식으로 바꿔달라는 요청. (2) 실적 4항목(T-NPS·생산성·통화시간)의 "이번 달 월평균"을 날짜별 입력과 별도로 직접 넣을 수 있게 해달라는 요청. (3) 날짜별로 체크박스를 둬서 특정 날짜를 저장 대상에서 뺄 수 있게 해달라는 요청.
- **항목 구조 개편**: `LGE_TOTAL_ATT_METRICS`를 `parts`(두 세부값)+`derivedFrom`(파생 여부) 기반 구조로 일반화. 항목 순서/구성이 TO(AS/성수기) → 총재직인원(AS/성수기, **자동계산**) → AS재직인원(관리자/상담사) → 성수기재직인원(관리자/상담사) → 상담사투입인원(AS/성수기)으로 바뀜. "관리자재직인원"/"상담사재직인원" 항목은 없어지고 저장 키도 `관리자재직인원_AS` 등에서 `AS재직인원_관리자` 등으로 **완전히 교체**됨(사용자 확인 후 진행 — 개편 이전 저장분은 관리자/상담사 분해값이 없어 재입력 필요). `CENTER_KPI_DEFS.lge_total`도 새 키 기준으로 함께 수정.
- **총재직인원 자동계산**: 더 이상 직접 입력하지 않고 AS 칸=AS재직인원 합계, 성수기 칸=성수기재직인원 합계를 읽기전용으로 자동 반영. `lgeTotalRecalc()`가 AS재직인원/성수기재직인원 입력이 바뀔 때 총재직인원까지 한 번에 연쇄 재계산하도록 파생 관계를 일반화(테스트 중 무한재귀 버그를 발견해 즉시 수정 — 재귀 호출을 "실제로 참조 관계가 있을 때만" 실행하도록 가드 추가).
- **월평균 실적 입력**: "시작일" 입력칸 위에 T-NPS/생산성_INOUT/생산성_IN/통화시간 4개의 "이번 달 월평균" 입력칸 추가(선택 입력). 저장하면 날짜별 데이터와 별개로 시작일이 속한 달의 1일 데이터에 `_월평균` 접미사 키로 붙어서 저장됨(`manual-entry-bulk` 재사용, 신규 백엔드 액션·SQL 없음). "기간 적용" 시 그 달 1일에 이미 저장된 값이 있으면 자동으로 불러와 표시.
- **날짜별 제외 체크박스**: 표 왼쪽에 "제외" 체크박스 추가. 체크한 날짜는 "전체 저장" 시 요청에서 빠짐(이미 저장된 DB 데이터를 지우는 게 아니라 이번 저장에서만 제외하는 용도 — 사용자 확인 후 이 방식으로 결정).
- **검증**: `node --check` 통과. 로컬 정적 서버에서 가짜 이력 데이터로 (1) 이월값이 새 키 구조로 정확히 채워지는지 (2) 총재직인원이 읽기전용으로 자동 계산되는지 (3) AS재직인원 값을 수정하면 총재직인원까지 연쇄 재계산되는지 (4) 월평균 기존값이 프리필되는지 (5) 제외 체크한 날짜가 실제 저장 요청(payload)에서 빠지고 월평균이 월 1일 데이터에 병합되는지까지 전부 확인함. 실제 Supabase 데이터로는 재검증 필요.
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-17 (2차) — LG전자통합 데이터입력 일괄입력 UI 개편
- **요청 배경**: 날짜별 체크박스로 선택하고 필드 하나+값 하나를 골라 "선택 날짜에 일괄 적용"하던 기존 방식(2026-07-16 2차)을 제거하고, 표 맨 위에서 항목별로 값을 바로 입력하는 방식으로 바꿔달라는 요청.
- **변경**: `generateLgeTotalRows()`에서 날짜별 체크박스 열과 "전체선택/전체해제" 버튼, 필드 선택 드롭다운+값 입력+"선택 날짜에 일괄 적용" 버튼을 모두 제거. 대신 표(tbody) 맨 위에 "일괄입력" 행을 추가해 AS/성수기 10칸 + 실적 4칸(T-NPS/생산성 IN+OUT/생산성 IN/통화시간)에 각각 값을 입력할 수 있게 하고, 칸마다 "전체반영" 버튼을 둬서 그 항목만 표에 보이는 모든 날짜에 채워지도록 함(`applyLgeTotalColumn`). 표 아래에는 "⚡ 전체 항목 전체반영" 버튼을 추가해, 일괄입력 행에 값이 채워진 항목을 전부 한 번에 전체 날짜에 반영(`applyLgeTotalAllColumns`). 기존 `lgeTotalToggleAll`/`applyLgeTotalBulk` 함수는 삭제.
- 날짜 개별 선택 개념이 없어져 항상 "지금 표에 나와 있는 전체 날짜"가 적용 대상이 됨(기존처럼 기간을 좁혀 표를 만들면 그 기간 전체가 대상).
- 저장(`saveLgeTotalRows`) 로직은 무변경 — 일괄입력 행 값은 실제 저장 대상이 아니라 각 날짜 행의 입력칸을 채우는 용도일 뿐이라 기존 저장 흐름 그대로 재사용됨.
- SQL 변경 없음. `node --check` 문법 통과, 로컬 정적 서버로 렌더링·항목별 전체반영·전체 항목 전체반영 동작 확인함(가짜 seed 데이터로 테스트, 실제 Supabase 데이터로는 재검증 필요).
- `docs/FEATURE.md` 15번 섹션 갱신함.

## 2026-07-17 — 파일 분리: admin.html → admin.html + style.css + app.js
- **목적**: 단일 파일이 6,500줄까지 커지면서 기능 추가/버그 수정 때마다 전체 파일을 훑어야 해 토큰 소모가 컸음. 토큰 절감 3가지 제안 중 1순위(파일 분리)를 사용자가 선택해 진행.
- **변경**: 기존 `admin.html`의 `<style>...</style>` 내용을 `style.css`로, 거대한 `<script>...</script>`(6,300여 줄) 내용을 `app.js`로 각각 추출. `admin.html`은 `<link rel="stylesheet" href="style.css">` + `<script src="app.js"></script>`만 남긴 얇은 뼈대가 됨.
- **기능/화면 변경 없음**: 브라우저가 파일을 나눠서 불러오는 것뿐, 화면 구성·동작 방식·기능은 100% 동일. `app.js`는 `node --check`로 문법 검증 통과.
- **주의(배포)**: 이제 세 파일을 **같은 폴더에 함께** 두 배포처 모두에 올려야 함. `style.css`/`app.js` 없이 `admin.html`만 올리면 화면이 깨지거나 완전히 빈 화면이 됨. `DEPLOY-CHECKLIST.md`/`SYSTEM.md`/`AGENTS.md`에 이 구조 반영해 갱신함.
- Chart.js/XLSX CDN 스크립트, jsdelivr 실패시 unpkg 폴백용 부트스트랩 스크립트는 여전히 `admin.html`에 인라인으로 남아있음(순서 민감성 때문에 그대로 유지).
- **앞으로의 작업 방식**: 로직 수정은 `app.js`만, 스타일 수정은 `style.css`만 전체 교체해서 드리면 됨 — `admin.html`은 거의 안 바뀜.

## 2026-07-16 (2차) — 화면 반응속도 개선 + LG전자통합 일괄입력
- **화면 반응속도**: 센터 전환/새로고침시 캐시(세션 메모리 + localStorage)가 있으면 네트워크 응답을 기다리지 않고 먼저 그 데이터로 화면을 그리고, 실제 최신 데이터가 도착하면 **캐시와 다를 때만** 조용히 재렌더링(같으면 아무 것도 안 함). `loadOverviewForCurrent()`가 "데이터가 실제로 바뀌었는지" boolean을 반환하도록 변경, `selectCenter()`/`init()`이 이를 활용. 응답이 늦게 와서 그 사이 다른 센터로 이동한 경우 옛 응답으로 화면을 덮어쓰지 않도록 가드 추가.
- **LG전자통합 일괄입력**: 날짜별 체크박스 + 전체선택/해제 + "선택 날짜에 일괄 적용"(필드 선택 + 값 입력 → 체크된 날짜 전부에 한 번에 채움, AS/성수기면 합계도 같이 재계산).
- LG전자통합 데이터입력 미반영 문의는 새로고침으로 해결됨(배포/캐시 지연이었던 것으로 추정, 코드 문제 아님).

## 2026-07-16 — LG전자통합 신규 센터
- 기존 LG전자AS/LG전자성수기는 그대로 두고, **완전히 독립된 신규 센터 "LG전자통합"(`lge_total`)** 추가. 두 원본 센터 데이터를 자동으로 끌어오지 않음(사용자 확인: "완전히 새롭게 만든다").
- 데이터입력은 파일첨부/붙여넣기가 아니라 **항목별 숫자 입력 전용 폼**으로 구성(`renderIntegratedFormEntry`, `INTEGRATED_FORM_CENTERS`). 이 센터를 선택하면 기존 추출/붙여넣기 카드는 아예 안 뜨고 이 폼만 나옴.
- 입력 항목: 날짜 / TO·총재직인원·관리자재직인원·상담사재직인원·상담사투입인원(각각 AS·성수기·합계) / T-NPS / 생산성_INOUT / 생산성_IN / 통화시간.
- TO·재직인원·투입인원 5개 항목은 **직전 저장일 값을 자동 이월**(사람이 확인한 의미: AS/성수기 값은 인원변동 있을 때만 수정하면 되도록), 합계는 AS+성수기 실시간 자동계산. T-NPS/생산성/통화시간은 이월 없이 매일 새로 입력.
- 저장/조회/수정/삭제는 기존 `manual-entry-bulk`/데이터조회 패널을 그대로 재사용(신규 백엔드 액션 없음, index.ts 변경 없음). "수정하기"만 이 센터 전용 폼으로 불러오도록 프론트엔드에서 분기.
- **SQL 신규**: `schema_addendum_11_lge_total_center.sql` — `center_config`에 `lge_total` 센터 등록(기본 비밀번호 000000).
- 알려진 제한: 대시보드 상단 "전체 재직인원(TO대비) %" 요약카드는 이 센터에 아직 없음(TO 자체가 매일 값이 바뀌는 입력 항목이라 기존 고정-TO_TARGET 구조와 안 맞음). 주요지표 리스트(일평균/누적평균)는 정상 표시됨.
- 검증 필요: SQL 실행 → 사이드바 노출 확인 → 기간 적용시 전날 값 이월 확인 → 합계 실시간 계산 확인 → 저장 후 수정 플로우 확인.

## 2026-07-15 — 🔴 장애: 사이트 전체 먹통(센터 목록 빈 화면) 원인 및 수정
- **증상**: `report.깡비서.kr`/`kkangbi-report.vercel.app` 둘 다 사이드바 센터 목록이 완전히 빈 화면으로 나옴. Supabase Table Editor에서 `center_config`/`center_daily_performance` 데이터는 그대로 있음을 확인 — **DB 데이터 유실이 아니라 Edge Function이 요청을 처리하지 못하는 상태**였음.
- **증거**: 브라우저 콘솔에 `Access to fetch ... has been blocked by CORS policy` 에러. 정상이라면 이 함수는 OPTIONS 프리플라이트에도 항상 CORS 헤더를 반환하도록 짜여 있어서, 이 에러는 **함수가 요청 처리 시작 전(모듈 로드 단계)에 죽고 있다는 신호**였음. Edge Function 로그의 "booted → 수 초 내 shutdown 반복"도 같은 크래시 루프 정황.
- **원인**: `index.ts` 최상단의 `import mammoth from 'https://esm.sh/mammoth@1.6.0';`(AI 기능 추가 이전부터 있던 기존 코드, 제가 건드리지 않은 줄)에서 `deno check`가 `TS1192: has no default export` 에러를 냄. esm.sh가 서빙하는 mammoth 타입 선언이 최근에 바뀌면서, 원래 잘 동작하던 코드가 배포 시점의 타입체크를 통과하지 못하게 된 것으로 추정.
- **수정**: `import mammoth from '...'` (default import) → `import * as mammothNs from '...'; const mammoth: any = (mammothNs as any).default ?? mammothNs;`로 변경. 런타임 동작은 동일, 타입체크만 회피.
- **검증**: 로컬에 Deno를 설치해 실제로 `deno check index.ts`를 돌려 수정 전 에러 재현 → 수정 후 에러 0건(exit code 0) 확인. `esbuild`로 순수 구문 오류도 별도 확인(이상 없음).
- **후속 조치 필요**: 재배포 후 (1) 사이드바 센터 목록 복구 확인 (2) 이번 기회에 AI 보조기능/즉시발송도 함께 재검증 (3) 혹시 백업해둔 이전 index.ts로 이미 롤백하셨다면, 이번 수정본으로 다시 교체 배포.

## 2026-07-13 (9차)
- **시크릿 통일**: AI 보조기능이 새로 만든 `SOGANG_MOT_API_URL`/`SOGANG_MOT_API_KEY` 대신, 다른 kkangbi 프로젝트에서 이미 쓰던 `MOT_GATEWAY_URL`/`MOT_GATEWAY_KEY`를 사용하도록 변경(값은 동일하다고 확인됨). 새로 등록하셨던 `SOGANG_MOT_API_URL`/`SOGANG_MOT_API_KEY`는 이제 안 쓰이니, 정리하려면 Supabase Secrets에서 삭제하셔도 됩니다(안 지워도 무해함).
- **SendGrid 관련 확인**: 시크릿 목록 확인 결과 `SENDGRID_API_KEY`는 이미 등록되어 있음. 지난 "0건 발송" 원인이 키 누락이 아니라 **`SENDGRID_FROM_EMAIL` 미등록으로 인한 발신자 미인증(SendGrid 403)일 가능성**으로 정정 — 재배포 후 즉시발송 실패 사유 컬럼에서 실제 원인 확인 필요.
- **즉시발송 센터별/전체 분리**: "⚡ 이 센터만 즉시발송"(알림설정에서 선택된 센터 1곳만) / "⚡⚡ 전체 센터 즉시발송"(활성 센터 전체) 버튼으로 분리. 백엔드 `send-notification-now`가 `center_code`를 선택적으로 받아 `runNotificationCheck(settings, forceSend, centerCodeFilter)`로 대상 범위를 좁힘 — `check-and-notify`(매시 크론)는 기존처럼 필터 없이 전체 대상 그대로 동작.

## 2026-07-13 (8차) — 즉시발송 결과 진단 개선
- **문의 배경**: 즉시발송 테스트에서 "대상 3건 중 0건 성공"이 나왔는데, "대상 건수"(주의/경고 조건에 걸린 센터 수)와 화면에 보이는 "담당자 건수"(알림설정에서 선택한 센터 1곳만의 담당자 수)가 서로 다른 스코프라 혼동이 있었음 + 발송 실패 사유가 전혀 안 보여서 원인 파악 불가.
- **개선**: `sendNotificationEmail()`이 이제 성공/실패만이 아니라 **실패 사유**(`SENDGRID_API_KEY 시크릿 없음` / `SendGrid HTTP 4xx·5xx 응답 본문` / `네트워크 오류`)까지 반환. `runNotificationCheck()`가 이를 `notification_log`(`send_ok`, `send_error` 컬럼 신규)에 기록하고, "즉시 발송" 결과 화면도 집계 숫자 한 줄이 아니라 **센터별 표**(센터명/단계/성공·실패/실패사유)로 보여주도록 변경.
- **SQL 추가**: `schema_addendum_10_notification_manual.sql`에 `send_ok boolean`, `send_error text` 컬럼 추가(기존 `is_manual`과 함께 한 파일에 정리).
- 검증: 재배포 후 즉시발송을 다시 눌러 실패 사유 컬럼에 `SENDGRID_API_KEY 시크릿이 설정되지 않았습니다.`가 뜨는지부터 확인 — 뜬다면 Supabase Secrets에 `SENDGRID_API_KEY`/`SENDGRID_FROM_EMAIL`을 등록해야 발송이 됨.

## 2026-07-13 (7차) — index.ts 실제 병합 완료 + 즉시발송 + 로딩속도 개선
- **index.ts 전체 병합**: 사용자가 실제 Edge Function 소스(`index.ts`)를 제공해줘서, 지난번 병합용 스니펫이 아니라 **완전한 파일**로 AI 보조기능 3개 액션(`ai-suggest-xlsx-mapping`, `save/get-xlsx-field-override`, `ai-summarize-issues`)을 실제 인증 헬퍼(`isCenterOrWorkspaceAuthorized`/`isWorkspaceAuthorized`)와 응답 헬퍼(`json`)를 그대로 사용해 병합했습니다. `checkAuth(...)` 같은 임시 자리표시자는 이제 없습니다. `schema_addendum_9_ai_provider.sql` 실행 + Secrets 2개(`SOGANG_MOT_API_URL`/`KEY`) 등록 + 재배포만 하면 바로 동작합니다.
- **알림 설정에 "⚡ 즉시 발송" 버튼 추가**: 발송시각/반복주기/중복방지 조건을 모두 건너뛰고 지금 조건(며칠째 미업로드)에 맞는 센터에 바로 발송(`send-notification-now` 액션). 매시 크론이 쓰던 로직을 `runNotificationCheck()`로 공용화해서 `check-and-notify`와 코드 중복 없이 재사용. 발송 로그에 즉시발송 여부 기록(`notification_log.is_manual`, `schema_addendum_10_notification_manual.sql`).
- **초기 로딩(버퍼링) 개선**:
  - `admin.html` 상단의 Chart.js/XLSX `<script>` 태그에 `defer` 추가 — 두 라이브러리 다운로드가 첫 화면 렌더를 막지 않게 됨(두 라이브러리는 전부 함수 내부에서만 쓰여서 안전함을 확인 후 적용).
  - 자주 안 바뀌는 조회성 API 6종(`schema`, `get-notification-settings`, `list-kpi-settings`, `list-kpi-monthly-targets`, `list-monthly-to`, `centers-manage-list`, `get-xlsx-field-override`)에 짧은 캐시(`jsonCached`, `private, max-age=20`) 적용. 실적/근태/자료함 등 실시간성이 중요한 API는 기존처럼 `no-store` 유지.
- **SQL 신규**: `schema_addendum_10_notification_manual.sql` (`notification_log.is_manual` 컬럼 추가)
- 검증: 재배포 후 (1) AI 두 기능 정상 케이스, (2) 알림 즉시발송 버튼, (3) 초기 진입 체감 속도를 실제로 확인 필요.

## 2026-07-13 (6차) — AI Provider 단순화: 서강MOT 단일 Provider(GPT5.5 고정)
- **요청 반영**: Claude API 등으로의 자동 failover를 제거하고, **서강MOT API 단일 Provider**로만 AI 기능(엑셀 매핑 제안/이슈 요약)을 호출하도록 변경. 모델은 선택 기능 없이 항상 GPT5.5로 고정.
- **크레딧 소진 등 실패 시 동작 변경**: 이전엔 다른 Provider로 조용히 전환했지만, 이제는 실패하면 화면에 "⚠️ AI 기능을 지금 사용할 수 없습니다(크레딧 소진 등)" 배너를 띄우고, "기존 방식(직접 입력/붙여넣기, 자동추출·저장)은 그대로 쓸 수 있다"는 안내 문구를 함께 보여준다(`renderAiUnavailableNotice()`). 엑셀 매핑 기능과 이슈 요약 기능 양쪽에 동일하게 적용.
- **기존 기능 영향 없음 재확인**: 데이터 입력·자동추출·저장(manual-entry-bulk 등)은 애초에 AI를 거치지 않는 별도 경로라, AI 실패 여부와 무관하게 100% 그대로 동작한다.
- `edge-function-addendum-ai-provider.ts`를 v2로 갱신(Claude 관련 코드 전부 제거, `AiUnavailableError`로 실패 사유를 프론트엔드에 명확히 전달). `schema_addendum_9_ai_provider.sql`은 주석만 정정(테이블 구조는 동일).
- **미완료는 여전히 동일**: 백엔드(index.ts) 병합 + Secrets 등록(`SOGANG_MOT_API_URL`/`SOGANG_MOT_API_KEY`만 필요, `ANTHROPIC_API_KEY` 불필요해짐) + 재배포.

## 2026-07-13 (5차) — AI 보조기능 신규 (🟡 프론트엔드만 완료, 백엔드 미배포)
- **엑셀 양식 변경 자동대응(KB손보부천)**: 자동추출이 실패("일자별 데이터를 찾지 못했습니다")하면 "🤖 AI로 양식 분석하기" 버튼 노출 → 헤더 텍스트+기존 필드정의를 AI에 보내 새 헤더 키워드 매핑 제안 → 사용자 검토 후 저장하면(`center_xlsx_field_override`) 다음부터 AI 호출 없이 자동 적용(`getEffectiveXlsxFields`, `loadXlsxFieldOverride`).
- **이슈 히스토리 AI 요약**: 이슈및히스토리 화면에 "🤖 AI 요약" 버튼 추가, 최근 이슈 최대 50건을 반복패턴/미해결추정/특이사항 관점으로 요약(`summarizeIssuesWithAI`).
- **AI Provider 정책**: 서강MOT API(1순위) 호출 → 크레딧부족/RateLimit/Timeout/장애 시 Claude API로 자동 failover(백엔드에서 처리, 사용자는 인지 못함). 비용민감도 원칙에 따라 AI는 "추출 실패 시"/"사용자가 요약 버튼을 누를 때"만 호출되고, 평소 데이터 추출·저장 흐름에는 전혀 관여하지 않음(기존 규칙기반 로직 100% 유지).
- **미완료(다음 세션 필요)**: `schema_addendum_9_ai_provider.sql` 실행, `edge-function-addendum-ai-provider.ts`를 실제 `index.ts`에 병합(인증 헬퍼 이름 교체 필요), 서강MOT 실제 API 스펙 확인 후 `callSogangMOT()` 조정, Function Secrets 4개 등록(`SOGANG_MOT_API_URL/KEY/MODEL`, `ANTHROPIC_API_KEY`). **이 저장소에 Edge Function 원본(index.ts)이 없어서 AGENTS.md의 "전체 교체 파일 원칙"을 지키지 못하고 병합용 스니펫으로만 제공함 — 다음 세션에서 index.ts를 공유해주시면 전체 파일로 정리 가능.**

## 2026-07-13 (4차)
- **평택시청 일일업무보고**: 원래 자료함 업로드 로직이 아예 없어 이 문제와 무관했으나, 요청에 따라 다른 센터와 동일하게 "최종저장"(`saveEverything`, 일자별 실적+업무유형별 인입현황을 함께 저장하는 버튼)이 실제로 성공했을 때만 원본 HWPX 파일이 자료함에 반영되도록 신규 추가. 추출만 하고 저장하지 않으면 자료함에 올라가지 않음.
- 일자별 실적 저장(`saveAllRows`)이 성공하면 그쪽에서 자료함 반영을 처리하고, 일자별 실적표 없이 업무유형별 인입현황만 추출·저장된 경우에도 `saveEverything`에서 별도로 자료함 반영을 판단해 중복 업로드 없이 처리.
- `saveAllRows`/`saveCategoryRows`가 저장 성공 여부(boolean)를 반환하도록 변경 — 다른 호출부(버튼 onclick 등)는 반환값을 쓰지 않으므로 영향 없음.

## 2026-07-13 (3차) — 업로드 자료함 저장 시점 버그수정
- **버그수정**: 업로드 자료함(`archive-upload-file`)이 파일을 "선택/추출"만 해도 즉시 업로드되던 문제. 실적파일(LG전자 엑셀추출/KB손보정비 엑셀자동추출/KB손보부천 엑셀추출/평택 HWPX추출)과 근태파일(근태파일 업로드) 모두, 이제 파일은 추출 시점엔 `pendingPerfArchiveFile`/`pendingAttArchiveFile`에만 잠깐 담아두고, **실제 저장(전체 저장/근태만 저장/실적만 저장/실적+근태 일괄반영)이 성공한 시점에만** 실제로 자료함에 업로드된다. 저장에 실패하면 자료함에도 올라가지 않는다.
- 평택시청 일일업무보고(HWPX)는 원래도 이 문제가 없었음(자료함 업로드 로직 자체가 없음) — 변경 없음.
- 검증: 파일을 선택/추출만 하고 저장 없이 다른 화면으로 이동했을 때 업로드 자료함에 안 올라가는지, 실제 저장 버튼을 눌렀을 때만 올라가는지 확인 필요.

## 2026-07-13 (2차)
- **KB손보부천**: "근태만 저장"/"실적만 저장" 버튼을 표 아래 별도 위치에서 "전체 저장" 버튼 바로 옆으로 이동 (`manualSaveAttBtn`/`manualSavePerfBtn`, 정적 배치 후 `renderPreviewTable()`에서 표시/숨김만 제어). 근태+실적이 함께 있는 양식일 때만 나타나는 동작은 동일.
- 동작 재확인: "근태만 저장"을 누르면 `saveAllRows('attendance')`가 호출되어 `data-group==='attendance'`인 필드만 서버로 전송되고, 실적 필드는 요청에 아예 포함되지 않는다(반대도 동일) — 즉 한쪽만 정확히 반영되는 것이 맞음.

## 2026-07-13 데이터입력 기능 개선
- **KB손보부천**: 엑셀 자동추출에 기간 설정(추출 시작일/종료일) 추가 — 비워두면 기존처럼 파일 내 전체 기간 추출(`extractXlsxAndFill`). 지정하면 그 기간 날짜만 붙여넣기 칸에 채운다.
- **KB손보부천**: "일자별 실적 직접입력" 표에 근태(총원~제외보유계약)와 실적(인입호~) 컬럼이 함께 있는 경우, 표 미리보기 하단에 "근태만 저장"/"실적만 저장" 버튼이 추가로 나타난다(`renderPreviewTable`, `saveAllRows(groupFilter)`). 기존 "전체 저장"은 그대로 유지. 한쪽만 저장하면 다른 쪽 컬럼은 서버로 아예 전송하지 않아 기존에 저장돼 있던 값이 보존된다.
- **KB손보정비**: "RAW 엑셀 자동추출" 명칭을 "엑셀 자동추출"로 변경. KB손보부천과 동일하게 추출 시작일/종료일 필드 추가(`extractLongContractAndFill`). 추출한 기간(또는 지정한 기간)은 기존처럼 재직및투입현황 탭 이동 시 자동으로 동일 기간이 설정된다(`promptGoToAttendance`, 기존 기능 유지·재검증 완료).
- **KB손보정비/LG전자AS/LG전자성수기 공통**: 재직 및 투입현황 화면에 "실적만 저장" 버튼 신규 추가(`saveAttPerfOnly`). 기존 "근태만 저장"/"실적+근태 일괄 반영"은 그대로 유지. "실적만 저장"은 근태 기간과 겹치는 날짜의 실적만 반영하고 근태 값(TO/재직인원/투입인원)은 전혀 건드리지 않는다.
- SQL 변경 없음(모두 기존 `manual-entry-bulk` 액션의 그룹별 부분 반영 방식을 재사용).
- 검증: KB손보부천에서 근태만 이미 정확히 들어가 있는 날짜에 실적만 재추출→"실적만 저장"으로 반영해도 근태 값이 그대로인지 실제 데이터로 확인 필요.

## 2026-07-12 (4차)
- **버그수정 (진짜 원인 발견)**: 상단 핵심지표 카드의 "누적" 계열 수치가 분기누적/반기누적/연초누적 토글을 눌러도 안 바뀌던 문제. 원인은 `renderSummaryCards`/`renderKbjeongbiSummaryCards2` 호출 시 "누적" 인자로 항상 연초~현재월 고정 `cumulativeRows`를 넘기고 있었기 때문("일평균" 계열만 토글에 반응하고 있었음). 이제 분기누적/반기누적 토글일 때는 해당 분기·반기 범위(`quarterRows`/`halfRows`)로 좁혀서 누적 계산하도록 수정 (`cardCumRows`). 라벨도 "누적"→"분기누적/반기누적/연초누적"으로 동적 표기(`periodCumLabel()`).
- **KB손보정비(및 section 있는 센터 공통) 주요지표 표기 변경**: "총합계/고지의무/통지의무/목적물소멸" 등 섹션명을 별도 배지 줄로 보여주던 것을 라벨과 한 줄로 병합 (예: "총합계 접수건 166건 / 누적평균 174건 / 전월比 ▼26건, 13.8%"). `renderTrendList()` 공통 함수 수정이라 KB손보부천(제휴상담/장기손사)에도 동일 적용.
- **복사 버튼 아이콘화**: 센터별 대시보드의 모든 복사 버튼(요약카드 `sc-copy`, 지표추이 차트별 복사, 주요지표 복사, 표 복사)을 빨간 텍스트 버튼에서 중립 다크톤 아이콘 버튼(`copy-icon-btn`, hover 시 밝아짐)으로 교체. 클릭 동작(클립보드 복사 함수 호출)은 기존과 동일하게 유지, title 툴팁으로 어떤 항목을 복사하는지 안내.

## 2026-07-12 (3차) — DESIGN.md 기준 다크 리스킨
- **범위**: 시각(색상/배경/보더/라운드/타이포)만 변경, 기능·DOM 구조·필터·클릭 핸들러·데이터 로직은 전부 그대로 유지 (기능 불변 원칙 준수).
- 페이지 배경을 흰색/연회색(`#f4f5f7` 등) 계열에서 `#000000`(메인)/`#1d1d1f`(카드·패널 surface)/`#111113`(표 헤더·입력창 elevated) 다크 토큰으로 전면 교체.
- 텍스트/보더를 `#f5f5f7`(본문) · `#a1a1a6`/`#86868b`(보조) · `#2c2c2e`(보더) 토큰으로 통일.
- 상태색상 재매핑: 성공 `#34c759`, 경고 `#f5a623`, 위험 `#FF6B70`(danger), 하락표시 `#5ac8fa` — 다크 배경 대비 기준으로 톤 조정.
- 버튼(`.btn-primary/outline/ghost/secondary`)을 pill(`border-radius:999px`)로, 대형 카드(`.panel`,`.ws-card`,`.ws-kpi-card`,`.summary-card2` 등)는 `16~20px`, 팝오버/드롭다운은 `12px`로 라운드 확대.
- 워크스페이스 전체현황 추이차트 팔레트(`WS_BAR_COLORS`/`WS_LINE_COLORS`)와 KBJ 도넛차트 미처리 슬라이스 색을 다크 배경에서도 구분되도록 별도 조정 (기존엔 옅은 회색이라 다크 배경에서 거의 안 보였음).
- LG전자/평택시청/KB손보 등 **센터별 브랜드·차트 계열색(A50034, 303192, FFBC00, 545045 등)은 의도적으로 손대지 않음** — 그래프 시리즈 구분이라는 기능적 의미가 있는 색이라 리스킨 대상에서 제외.
- `docs/DESIGN.md`를 저장소에 신규 반영 (사용자 제공본).
- 검증: `node --check`로 JS 문법 오류 없음 확인. 실제 브라우저 렌더링(색 대비·가독성)은 배포 후 육안 확인 필요 — 특히 표 스크롤 시 sticky 첫 열 배경, 입력창(`input[type=date/number/text]`, `select`, `textarea`) 다크 배경 적용 여부를 우선 확인할 것.

## 2026-07-12 (2차)
- **사이드바 구조 개편**: 상단 고정 탭(대시보드/데이터입력/이슈및히스토리/TO및목표값설정) 제거 → 사이드바에서 선택된 센터 바로 아래에 서브메뉴로 노출 (`MAIN_TABS`, `renderCenterSubmenu()`). SQL 변경 없음.
- **버그수정**: 센터 전환 시 직전 센터에서 보던 탭·기간(월/단월 등)이 그대로 유지되던 문제 수정. 이제 센터를 바꾸면 탭=대시보드, 기간=단월, 월=이번달로 초기화됨 (`resetDashboardStateForNewCenter()`, `selectCenter()`/`promptCenterPassword()` 양쪽 적용).
- **이슈 및 히스토리**: 목록을 제목+날짜만 보이는 아코디언(`<details>`) 방식으로 변경, 클릭 시 상세내용 펼침 (전 센터 공통 함수 `renderIssueList()`라 5개 센터 모두 자동 적용).
- **KB손보정비 대시보드**: 주요지표에 "전체재직인원(TO대비)" 바로 아래 "상담사 투입현황"(`상담사_투입인원`) 지표 추가.
- **버그수정**: KB손보정비도 LG전자AS/성수기와 동일하게, 상담사 투입인원 0명인 날짜는 공휴일 여부와 무관하게 저장에서 자동 제외되도록 수정 (`skipZeroManual`에 `kbjeongbi` 추가).
- **LG전자AS/성수기 TO및목표값설정**: 통화시간(`통화시간_INOUT_초`) 등 duration 지표의 목표값을 `4:00:00`(시:분:초) 형식으로 입력 가능하도록 변경. 저장은 기존과 동일하게 초 단위 숫자, 화면 표시만 변환 (`DURATION_METRIC_KEYS`, `parseKpiTargetInput()`, `formatKpiTargetDisplay()`).
- **확인**: "다른 센터 대시보드 핵심지표 카드가 기간토글(단월/분기/반기/연초)에 반응하지 않는다"는 요청 건은 코드 재검토 결과 이미 `renderSummaryCards()`/`renderKbjeongbiSummaryCards2()`가 토글로 필터링된 데이터를 공통으로 사용하고 있어 전 센터 동일하게 동작 중임을 확인함 (별도 수정 없음 — 특정 센터에서 다르게 보이면 캐시/재접속 이슈일 가능성, 재현되면 스크린샷과 함께 재문의 필요).

## 2026-07-10 ~ 07-12
- **Google Drive 완전자동 연동** 추가: 센터별 폴더 등록 → 15분마다 폴링 → LG전자AS/성수기 실적·근태 완전자동 분석·반영. `schema_addendum_8_gdrive.sql`
  - 버그수정: 근태 자동반영 시 TO값이 두 센터 모두 19로 고정돼 있던 것을 AS=19/성수기=35로 수정, 투입인원 0명 날짜 제외 규칙 서버측에도 반영
- **스마트업로드** 추가: 파일 하나로 센터+실적/근태 자동판별 → 해당 화면 자동 이동+추출. 파일명 힌트(성수기/AS센터/정비/부천/평택) 우선 반영
- 사이드바 UI를 KT 스타일(다크톤, 섹션 구분: 메뉴/센터/관리)로 개편, 계정메뉴 드롭다운화, "워크스페이스" 명칭 → "관리자화면"으로 전면 변경
- **센터별 업로드 신호등 + 이메일 알림** 추가: 3/7일 기준 🟢🟠🔴, 4일째/8일째 담당자 메일 발송(SendGrid, 무료). `schema_addendum_7_upload_monitor.sql`
- **업로드 자료함** 추가: 원본 파일 자동 보관, 검색/다운로드/삭제. `schema_addendum_6_upload_archive.sql`
- 데이터입력 저장을 5건씩 배치 → `manual-entry-bulk`로 한번에 저장하도록 변경 (대량 데이터 저장 속도 개선)
- 버그수정: `categorySchemaCache` 전역변수 선언 누락으로 인한 "데이터 로드 실패" 오류

## 2026-07-08 ~ 07-09
- 전체현황 대시보드 지표별 정확한 산출근거 확인 및 수정: 생산성(IN+OUT) 단위 %→건, 전체재직인원(TO대비) 산출 소스를 상담사_투입인원 → 재직인원(전체)로 정정
- 지표추이 그래프: 생산성(IN)을 생산성(IN+OUT) 안에서 구분하는 파생 누적막대로 변경(단순 합산 아님), T-NPS 선 색상 LG RED 고정 버그 수정(임계값 없는 선이 무조건 회색으로 나오던 원인)
- LG전자성수기 TO/운영기준 정정: 공식 TO 상담사 35명(관리자 0명), 실제 운영기준 41명(관리자6+상담사35). `schema_addendum_4_seongsu_to.sql`
- LG전자AS/성수기 주요지표에 통화시간(IN+OUT) 추가(duration 단위 지원). `schema_addendum_5_talktime_kpi.sql`
- 데이터입력 UI 개편: 좌측 고정 가이드 + 우측 선택카드(파일업로드/직접입력/근태/업무유형별), 기준연도 강조박스, 파일 드래그앤드롭 지원

## 2026-07-07
- **LG전자AS·LG전자성수기 신규 센터 등록** (기존엔 lge만 있었음). `schema_addendum_3_lge_seongsu.sql`
- LG전자 KPI 엑셀 자동추출기(v2, 상시/한시 Total 판별 fallback 로직 포함) 이식, CDN 로딩 실패 대비 unpkg 폴백 추가
- LG전자·KB손보정비 근태 자동추출기 이식 (성명/업무/입사일/퇴사일 헤더 + YYMM 시트 인식)
- 실적 추출 완료 시 재직/투입현황 화면으로 자동 이동 + 추출 기간 그대로 전달
- 워크스페이스 이상징후 판정 기준을 "마지막 저장일"에서 "연초~최신월 누적 일평균"으로 변경
- 전체현황 추이그래프를 표 안 미니 스파크라인 → 표 아래 별도 카드(범례/축 포함) 방식으로 개편
- `schema_addendum_2.sql`(핵심지표 기본값), `schema_addendum_lge.sql`(LG전자 최초 등록)

## 그 이전 (초기 구축, 요약)
- KB손보부천/KB손보정비/평택시청 센터별 데이터입력(엑셀/HWPX 자동추출), 워크스페이스 전체현황, TO 및 목표값설정 화면 최초 구축
- 인수인계 기준: `handover.md`(구버전, 현재는 이 `docs/` 세트로 대체)
