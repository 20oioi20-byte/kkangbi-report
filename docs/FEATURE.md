# FEATURE.md — 구현 기능 목록 및 검증 체크리스트

상태 표기: ✅ 구현+실사용 검증됨 · 🟡 구현됨(제한적 검증) · ⛔ 미구현(설계만 됨)

## 1. 센터 관리
- ✅ 센터 목록/추가/이름변경/순서변경 (사이드바)
- ✅ (2026-07-21) **센터 "삭제"를 "숨기기/다시 보이기"로 전환**: 기존 `center-delete`가 `center_config`를 물리 삭제하려다 하위 테이블 외래키 제약으로 500 에러가 나던 문제를 계기로, 삭제 대신 숨기기 방식으로 전환. 사이드바 센터 메뉴의 "삭제"가 "🙈 숨기기"로 바뀌었고(`hideCenterPrompt`), "⚙ 계정 ▾ → 🙈 숨긴 센터 관리"에서 숨긴 센터 목록을 보고 "다시 보이기"로 복원 가능(`unhideCenter`, `toggleHiddenCentersPanel`/`renderHiddenCentersPanel`). 숨긴 센터는 사이드바·전체현황(요약 카드·실적 데이터 모두)에서 제외되고(`visibleCentersMeta()`, `loadAllCentersOverview()`의 클라이언트 필터), 실적 데이터 자체는 DB에 그대로 남아 다시 보이게 하면 즉시 복원됨. 새 컬럼·새 백엔드 액션 없이 `center_config`에 이미 있던 `is_active` 컬럼과 기존 `action=center-update`를 그대로 재사용해서 구현 — **배포 없이 즉시 동작**. 숨긴 동안에는 그 센터의 업로드 링크(토큰)와 미업로드 알림도 함께 멈춤(index.ts가 `is_active`를 공통으로 검사하기 때문 — "당분간 안 쓰는 센터를 치워둔다"는 의도와 자연스럽게 맞음).
- ✅ 5개 센터: KB손보부천(`kbsonhae`), KB손보정비(`kbjeongbi`), LG전자AS(`lge`), LG전자성수기(`lge_seongsu`), 평택시청(`pyeongtaek`)
- ✅ (2026-07-12) 상단 고정 탭(대시보드/데이터입력/이슈및히스토리/TO및목표값설정) 제거 → 사이드바에서 현재 선택된 센터 바로 아래에 서브메뉴로 노출 (`MAIN_TABS`, `renderCenterSubmenu()`). 센터를 바꾸면 서브메뉴는 자동으로 해당 센터 것으로 교체됨.
- ✅ (2026-07-12) 센터 전환(사이드바 클릭 또는 비밀번호 인증 성공) 시 화면상태 초기화(`resetDashboardStateForNewCenter()`): 탭=대시보드, 기간=단월, 월=이번달로 리셋. 이전엔 직전 센터에서 보던 탭·기간이 그대로 유지되는 버그가 있었음.
- 검증: 사이드바에서 센터 클릭 → 비밀번호 프롬프트(비관리자) 또는 즉시 진입(관리자) 확인. 서브메뉴 탭 전환 시 다른 센터 서브메뉴가 사라지고 새 센터 서브메뉴만 보이는지 확인.

## 2. 데이터입력 — 실적
| 센터 | 방식 | 상태 |
|---|---|---|
| KB손보부천 | 엑셀 자동추출(고정 필드) + (2026-07-13) 추출 기간 설정 | ✅ |
| KB손보정비 | 엑셀 자동추출(구 "RAW 엑셀 자동추출") + (2026-07-13) 추출 기간 설정 | ✅ |
| 평택시청 | HWPX 일일보고 자동추출 | ✅ |
| LG전자AS | 엑셀 자동추출 (summary 시트, **상시** Total) | ✅ 실제 파일 검증 완료 |
| LG전자성수기 | 엑셀 자동추출 (summary 시트, **한시** Total) | ✅ AS와 동일 로직, targetGroup만 다름 |
| 공통 | 직접 붙여넣기(여러 날짜 한번에) | ✅ |

- 파일 추출 완료 시 "직접입력" 화면으로 자동 전환 + 미리보기 자동 표시 (`selectEntryMethod('paste')` + `parseMultiPaste()`)
- 저장은 `manual-entry-bulk` 액션으로 **한 번에 일괄 저장** (5건씩 나눠 보내던 방식 폐기)
- ✅ (2026-07-13) KB손보부천/KB손보정비: 엑셀 자동추출에 추출 시작일/종료일 지정 가능 (비워두면 기존처럼 파일 내 전체 기간).
- ✅ (2026-07-13) KB손보부천: 근태(총원~제외보유계약)·실적(인입호~) 컬럼이 한 표에 같이 있어, 미리보기 표에서 "근태만 저장"/"실적만 저장"을 선택해 한쪽만 반영 가능 (`saveAllRows(groupFilter)`) — 기존에 잘 들어간 근태는 그대로 두고 실적만 다시 반영하는 식의 부분 수정용.
- ✅ (2026-07-17 4차) **KB손보정비: 실적/근태 완전 분리**. 이전엔 엑셀 자동추출 후 `promptGoToAttendance()`가 자동으로 "재직 및 투입현황" 탭으로 이동시키고 그 화면에 "실적+근태 일괄 반영" 버튼까지 노출돼 있어 두 데이터를 한 번에 묶어 저장하기 쉬운 구조였음. 이제 KB손보정비는 엑셀 자동추출/붙여넣기가 끝나도 자동 이동하지 않고 "일자별 실적 직접입력" 화면에 머무르며, 사용자가 그 화면의 "전체 저장"으로 **실적만** 먼저 저장한다. "재직 및 투입현황" 화면은 KB손보정비에 한해 "근태만 저장" 버튼만 노출되고(`실적+근태 일괄 반영`/`실적만 저장` 버튼은 숨김), 그 화면에서는 **근태만** 저장된다. 두 저장 모두 자기 그룹(`group: 'attendance'` 또는 `'performance'`) 필드만 전송하므로, 실적을 먼저 올리고 근태를 나중에 올리거나 그 반대여도 서로의 값을 지우지 않는다(기존에 검증된 `manual-entry-bulk`의 부분 필드 병합 동작에 근거). LG전자AS/성수기는 기존 방식(일괄 반영 버튼 포함) 그대로 유지.
- ✅ (2026-07-17 4차) **KB손보정비: "목적물소멸" 건수 판정 방식 수정**(`lcClassify()`). 기존엔 의무위반사항이 정확히 "단순변경"이고 처리구분에 "목적물소멸"이 포함된 경우만 인정했음. 이제는 의무위반사항/처리구분 **둘 중 어느 값에든 "목적물소멸"이라는 단어가 포함**돼 있으면 무조건 목적물소멸 1건으로 집계한다 — "목적물소멸,담보소멸", "담보납입면제,목적물소멸,담보무효취소"처럼 다른 사유와 콤마로 같이 적힌 값도 전부 포함됨. ⚠️ 실제 엑셀 파일로 검증하지 못했음(AGENTS.md "추측 금지" 원칙 예외 — 파일이 없어 두 컬럼 모두에 안전하게 적용하는 방식으로 구현) — 다음 실제 파일 업로드 시 집계 건수가 늘어난 게 맞는지 확인 필요.
- ✅ (2026-07-17 4차) **평택시청: "일자별 실적 직접입력"/"업무유형별 현황" 모두에 항목별 직접입력 표 추가**(LG전자통합과 동일한 형태: 날짜범위 지정 → 일괄입력 행 + 항목별/전체 "전체반영" + 날짜별 "제외" 체크박스). 기존 텍스트 붙여넣기 UI는 그대로 유지되고, 이 표는 그 아래에 별도 패널로 추가된 완전히 독립적인 입력 경로(저장 버튼도 따로)다. 컬럼은 하드코딩하지 않고 서버가 내려주는 `rowSchema`/`categorySchema`를 그대로 사용(`renderPtDailyItemShell`/`renderPtCategoryItemShell`). 일자별 실적 쪽은 "투입인원"(근태)만 직전 저장일 값으로 이월되고 나머지(요청호/응답호/응대율/포기호/CPD)는 매일 새로 입력. 업무유형별 현황 쪽은 이월 없이 매일 새로 입력하며, 저장 시 "합계" 대비 비중(%)을 자동 계산해 `{key}_비중` 키로 함께 저장(`savePtCategoryItemRows`, 기존 `saveCategoryRows`와 동일한 계산식).
- ✅ (2026-07-17 6차 버그수정) **KB손보정비: 엑셀 여러 시트 합산 안 되던 버그 수정**(`extractLongContractAndFill`). 필수 컬럼을 가진 시트가 워크북에 여러 개 있으면(예: 월별로 시트가 나뉜 파일) 예전엔 가장 먼저 찾은 시트 하나만 읽고 나머지는 버렸음. 조건을 만족하는 시트를 전부 찾아 각각 파싱 후 레코드를 합침, 추출 완료 메시지에 합쳐진 시트 개수/이름을 표시.
- ✅ (2026-07-18, 실제 파일로 재검증) **KB손보정비: 데이터 중간에 재삽입된 헤더 행 무시하도록 수정**. 사용자가 실제 원본 파일(`KB손보정비센터_0114~0714_데이터.xlsx`)을 제공해 직접 확인한 결과, 이 파일은 시트가 1개뿐이라 위 다중시트 버그와는 무관했음 — 대신 데이터 중간(여러 조회결과를 이어붙인 지점)에 헤더 행이 그대로 한 번 더 삽입돼 있어서, "요청일"이라는 글자 자체가 날짜 값으로 잘못 들어가고 문자열 정렬 때문에 맨 뒤로 밀리며 "파일 내 전체 기간" 표시가 깨지는 실제 버그를 발견·수정(의무위반사항/처리구분 값이 정확히 헤더 텍스트와 같은 행은 데이터가 아니라 재삽입된 헤더로 보고 건너뜀). 단, **"1/14~3/23만 빠진다"는 신고 증상 자체는 이 파일로 시뮬레이션해도 재현되지 않았음**(파싱 로직만 놓고 보면 이미 1/14~7/14 전체가 정상 추출됨) — 다음에 또 일부 기간만 반영되면 "추출 시작일/종료일" 입력칸에 이전 값이 남아있지 않은지부터 확인 필요(값이 있으면 결과 메시지에 "· 지정 기간만 반영"이 함께 표시됨).
- ✅ (2026-07-18 2·3차) **🔴 관리자 계정에서 실제로 저장된 데이터가 대시보드/조회 어디에도 안 보이던 버그 수정**. 근본 원인은 `loadOverviewForCurrent()`(특정 센터 화면 전용 데이터 로더)가 관리자(`workspaceUnlocked`)일 때 센터 필터 없이 "전체 센터를 통틀어 최근 300건"을 요청하고 있었던 것 — 센터가 여러 개면 다른 센터들의 최근 데이터만으로 300건이 채워져서, 지금 보는 센터의 오래된 데이터는 실제로 저장돼 있어도 대시보드·조회 화면 어디에도 안 나왔음(2차에서는 "조회" 패널만 `queryDataManage()`를 별도로 고쳤는데, 대시보드는 여전히 안 보인다는 재신고를 받고 3차에서 근본 원인을 찾음). 워크스페이스 전체현황(홈)은 이 함수를 쓰지 않고 별도의 `loadAllCentersOverview()`를 쓴다는 걸 확인했으므로, `loadOverviewForCurrent()`는 관리자 여부와 무관하게 항상 `&center=<현재 센터>`로 한정해서 요청하도록 수정 — 이제 대시보드·데이터입력·조회 전부 정상적으로 그 센터의 데이터를 본다.
- 검증: 실제 KPI 파일로 추출 → 미리보기 표 숫자가 원본과 일치하는지 확인. 기간 설정 필터링, KB손보정비 실적/근태 분리 저장(순서 바꿔가며), 목적물소멸 집계, 평택시청 항목별 직접입력 두 표, 여러 시트 합산/헤더 재삽입 행 처리 모두 확인 완료(헤더 재삽입은 실제 파일로 검증, 나머지는 재검증 필요).

## 3. 데이터입력 — 근태(재직/투입현황)
- ✅ KB손보정비/LG전자AS/LG전자성수기: 근태파일 업로드 시 자동 추출 (성명/업무/입사일/퇴사일 헤더 + `YYMM` 월별 시트 인식)
- ✅ 파일 선택 즉시 자동 추출 (`onchange="extractAttFile()"`)
- ✅ 이미 지정된 기간(실적 추출에서 넘어온 기간)이 있으면 덮어쓰지 않고 유지
- ✅ (2026-07-12) KB손보정비/LG전자AS/LG전자성수기: 상담사 투입인원(수동입력값) 0명인 날짜는 공휴일 여부와 무관하게 저장에서 자동 제외 (`skipZeroManual`, `saveAttRangeRows`/`saveAttAndPerfCombined` 양쪽 적용). 이전엔 LG전자AS/성수기만 적용되고 KB손보정비는 빠져 있었음.
- ✅ (2026-07-13) LG전자AS/LG전자성수기: "근태만 저장"/"실적+근태 일괄 반영"에 이어 "실적만 저장" 버튼 신규 추가(`saveAttPerfOnly`). 근태 기간과 겹치는 날짜의 실적만 반영하고 근태(TO/재직인원/투입인원)는 건드리지 않음.
- ✅ (2026-07-17 4차, 위 섹션2 "실적/근태 완전 분리" 참고) KB손보정비는 이 화면에서 "근태만 저장" 버튼만 노출됨 — "실적+근태 일괄 반영"/"실적만 저장"은 숨김 처리(`generateAttRangeRows()`의 `showCombinedAndPerfOnly` 분기). 자동추출 후에도 이 화면으로 자동 이동하지 않음(`promptGoToAttendance`가 kbjeongbi는 조기 반환).
- ✅ (2026-07-18 4차) **KB손보정비 전용**: 날짜별 표에 "제외" 체크박스 열 추가(`.att-exclude`, LG전자AS/성수기는 변경 없음 — 사용자가 LG전자는 빼달라고 확인). 체크한 날짜는 행이 흐리게 표시되고 "근태만 저장" 시 저장 대상에서 제외됨(`saveAttRangeRows`). 투입인원 0명 자동제외와는 별개로 결과 메시지에 구분 표시.
- ✅ 기본 TO: LG전자AS `19`(관리자4+상담사15), LG전자성수기 `35`(공식, 관리자0+상담사35) / 운영기준 재직인원 기본값 `41`(관리자6+상담사35)
- 검증: 실제 근태파일(`2026_LG전자_AS센터_근태현황.xlsx`)로 6월 21영업일 전량 매칭 확인됨(첨부 파일로 검증한 이력 있음 — `CHANGELOG.md` 참고). KB손보정비는 투입인원 0명 날짜가 포함된 실제 파일로 재검증 필요. "실적만 저장"도 실제 저장 데이터로 근태 미변경 여부 재검증 필요.

## 4. 워크스페이스 전체현황 (홈)
- ✅ 상단 카드: 전체 센터 수 / 전체 월평균 재직인원 / 주의 센터 수(이상징후 감지)
- ✅ 이상징후 판정 기준: "마지막 저장일"이 아니라 **연초~최신월 누적 일평균**(주말·공휴일 제외) 기준
- ✅ 핵심지표 표시: 이번달 값 + 누적값 함께 표기, 지표별 단위(%/건/duration) 정확히 반영
- ✅ 센터별 추이그래프: 회색계열 통일(단, LG전자는 센터별 대시보드에서만 LG 브랜드컬러 사용)
- ✅ (2026-07-21) **센터별 핵심지표 추이(최근 6개월) 그래프에 LG전자통합 추가 + 신규 센터 자동 반영**: `CENTER_CARD_TREND['lge_total']` 추가(생산성 IN/OUT 누적막대 + T-NPS 선, lge/lge_seongsu와 동일 패턴). 별개로, 지금까지는 이 그래프에 나오려면 `CENTER_CARD_TREND`에 센터별로 수동 등록이 꼭 필요했는데, 앞으로 새 센터가 생겨도 별도 등록 없이 자동으로 나오도록 `getCardTrendConfig()` 함수를 추가 — `CENTER_CARD_TREND`에 커스텀 등록이 없으면, 그 센터의 대시보드용 `CENTER_CHART_CONFIG`(첫 번째 그룹의 막대·선 구성)를 그대로 가져와 자동으로 카드를 만든다. 새 센터를 추가할 때 대시보드 자체가 동작하려면 `CENTER_CHART_CONFIG` 등록이 어차피 필수이므로, 전체현황 카드만을 위한 별도 등록 단계가 사라짐(라벨을 다르게 부르거나 파생값을 써야 하는 센터는 기존처럼 `CENTER_CARD_TREND`에 직접 등록해 자동 생성을 덮어쓸 수 있음).
- ✅ (2026-07-21 2차) **센터별 핵심지표 추이에서 LG전자통합만 월평균 수동입력값 기준으로 표시**: 다른 센터는 일별 실측 데이터를 월 단위로 평균해서 그래프를 그리는데, LG전자통합은 대시보드 요약카드와 동일하게 데이터입력 화면에서 수동으로 입력해둔 `{key}_월평균` 값을 그대로 사용하도록 `drawWorkspaceSparklines()`의 `aggSeries()`에 lge_total 전용 분기 추가(기존 `avgMetricValue()` 재사용, 새 로직 없음). 다른 센터는 기존처럼 일별 실측 평균 그대로 — 동작 변화 없음.
- ✅ 🚦 센터 실적 업데이트현황 카드 (신호등, 섹션 8 참고)
- 검증: 워크스페이스 진입 → 핵심지표 옆 "(누적 xx)" 괄호값이 실제 연초 데이터 반영하는지 확인

## 5. 센터별 대시보드
- ✅ 요약카드(전체 운영요약 등) — 상단 기간토글(단월/분기누적/반기누적/연초누적)과 연동. (2026-07-12 4차) "일평균" 계열은 처음부터 정상 반영되고 있었으나, **"누적" 계열이 토글과 무관하게 항상 연초~현재월 고정이던 실제 버그를 발견해 수정**함 — 분기누적/반기누적 토글 시 해당 범위로 좁혀서 누적 계산(`cardCumRows`), 라벨도 "분기누적/반기누적/연초누적"으로 동적 표기.
- ✅ 지표추이 미니차트 — LG전자: 생산성(IN) + 생산성(OUT, 파생값) 누적막대 + T-NPS 선(LG RED)
- ✅ (2026-07-12) KB손보정비 주요지표(우측 패널)에 "전체재직인원(TO대비)" 바로 아래 "상담사 투입현황"(`상담사_투입인원`, people 타입) 추가
- ✅ (2026-07-12 4차) 주요지표 리스트에서 섹션이 있는 항목(예: 총합계/고지의무/통지의무/목적물소멸, 제휴상담/장기손사)은 섹션명을 라벨과 한 줄로 병합해서 표기 ("총합계 접수건 000건 / ..."), 별도 배지 줄 제거. `renderTrendList()` 공통 함수라 전 센터 적용.
- ✅ (2026-07-12 4차) 센터별 대시보드의 복사 버튼(요약카드/차트/주요지표/표) 전부 아이콘 버튼(`copy-icon-btn`)으로 통일, 클릭 시 기존과 동일하게 클립보드 복사.
- ✅ (2026-07-17 6차) **KB손보정비: 단월 일별 지표추이 그래프에서만 주말(토/일) 제외**(`drawMiniCharts`, `aggView === 'daily'` && `currentCenter === 'kbjeongbi'`일 때만 필터). 저장된 데이터·다른 기간 토글(분기/반기/누적)·다른 센터 그래프는 그대로 유지, 이 그래프 하나만 주말 데이터 포인트를 표시하지 않음.
- ✅ (2026-07-19 3차) **지표추이 미니차트의 일별 선 그래프가 주말/공휴일에 실제로 입력된 값까지 빠뜨리던 버그 수정**(`drawMiniCharts`의 `avgSeries`). 일별(daily) 뷰는 버킷마다 그 날짜 데이터 1건뿐인데도 "휴일·주말 제외 평균"(`avgExcludingHolidays`)을 그대로 적용해서, 그 1건이 주말이거나 `KR_HOLIDAYS_2026`에 등록된 공휴일(예: 제헌절 7/17)이면 실제로 값이 입력돼 있어도 무조건 null로 처리해 그래프에서 사라졌음(LG전자AS처럼 주말에도 실제 운영하는 센터에서 특히 체감됨). 일별 뷰는 이제 그 날짜의 실제 값을 그대로 보여주고(값이 없으면만 null), 여러 날을 묶는 주/월별 등 집계 뷰에서는 기존처럼 근무일 기준 평균을 유지. 전 센터 공통 적용.
- ✅ (2026-07-19 3차) **KB손보부천: "주요지표 (일평균/누적평균)" 리스트에서 제휴상담/장기손사의 "실적·응대호"·"응답율" 항목 숨김**(`CENTER_KPI_DEFS.kbsonhae`에서 두 항목 제거). "실적·인입호"/"SL"만 남음 — 요약카드·차트 등 다른 화면은 이 리스트 전용 설정(`CENTER_KPI_DEFS`)과 무관해 영향 없음.
- ✅ (2026-07-19 5차, 7차, 11차, 12차를 거쳐 13차에서 원래 한 줄 형태로 복원됨 — 아래 참고) "주요지표 (일평균/누적평균)" 리스트 레이아웃을 두 줄 분리 → 테두리 박스 → 칩(chip) 흐름형 → 정렬 표 순으로 계속 실험했었음.
- ✅ (2026-07-19 13차) **주요지표 리스트를 원래의 "라벨+일평균/평균/전월비/전년동월비 한 줄 표기" 형태로 복원 + 말줄임(...)·클릭 펼치기 추가**: 여러 레이아웃(표/칩 등)을 시도해봤지만 결국 라벨 뒤에 값들을 한 줄로 쭉 이어붙이는 원래 형태(`compare-badge`/`compare-yoy` 인라인 배지)로 되돌림. 다만 항목별 내용 길이가 서로 달라 화면이 지저분해 보이는 문제를 별도로 해결하기 위해, 각 행은 기본적으로 **한 줄만** 보이도록 `white-space:nowrap`+`text-overflow:ellipsis`로 말줄임 처리하고, 그 줄을 **클릭하면** `expanded` 클래스가 토글되어 잘린 내용 없이 전체가 줄바꿈되어 펼쳐짐(다시 클릭하면 원래대로 접힘). 표 레이아웃(12차)에서 쓰던 `.trend-tbl`/`.tbl-scroll` CSS는 이 복원으로 더 이상 안 쓰여 제거. 전 센터 공통 함수라 자동 적용됨.
- ✅ (2026-07-19 14차) **주요지표 리스트 명칭 정리(2차)**: 전 센터 공통으로 "전월비"→"전월比"/"전년동월비"→"전년동월比"(한자 比). KB손보부천: 전체재직인원→재직인원, 제휴상담 근태→제휴인원, 제휴 인입호→제휴인입호, SL→제휴SL, 장기손사 근태→손사인원, 손사 인입호→손사인입호, SL→손사SL(`CENTER_KPI_DEFS.kbsonhae`, 섹션 접두사 없이 라벨을 직접 그 이름으로 지정). KB손보정비: 전체재직인원→재직인원, 상담사 투입현황→투입인원(나머지 총/고지/통지/목적물 접수건은 그대로). LG전자통합: TO·AS재직인원·성수기재직인원 3개 항목은 리스트에서 완전히 제거(`CENTER_KPI_DEFS.lge_total`에서 삭제, 데이터입력·데이터표에는 영향 없음 — 이 리스트 전용 설정만 제거된 것), 총재직인원→재직인원, 상담사투입인원→투입인원, 통화시간(IN+OUT)→통화시간, T-NPS·생산성(IN+OUT)·생산성(IN)은 그대로 유지.
- ✅ (2026-07-19 10차) **KB손보정비: 상단 지표카드(Total/고지의무/통지의무/목적물소멸 4개)를 "일평균 접수건·처리건·미처리건" + "월누적 접수건·처리건·미처리건" 구성으로 재편**(`renderKbjeongbiSummaryCards2`). 기존엔 2번째 줄에 "연초누적 접수건/처리건/처리율/미처리건"이 항상 연초 기준으로 고정돼 있었는데, 이제 기본(단월) 토글에서는 "월누적"(이번 달 지표, 공휴일·주말 포함 전체 합계)을 보여주고, 상단 기간토글을 분기누적/반기누적/연초누적으로 바꾸면 라벨·데이터 모두 그 기간 기준으로 자동 전환됨(`periodCumLabel()`/`cardCumRows` 재사용). "일평균 미처리건"은 신규 지표로, 날짜별 (접수-처리) 값을 근무일 기준으로 평균해서 계산. 처리율은 이 카드에서 제거(도넛차트로 이미 별도 표시 중이라 중복).
- ✅ (2026-07-19 10차) **KB손보정비: 지표추이 그래프에서 주말뿐 아니라 공휴일도 함께 숨김**(`drawMiniCharts`, `isWeekendOrHoliday()`로 교체 — 기존엔 요일만 체크해서 `KR_HOLIDAYS_2026`에 등록된 평일 공휴일(예: 제헌절 7/17)은 그래프에 그대로 남아있었음). 데이터 자체는 그대로 저장·유지, 그래프 표시에서만 제외.
- ✅ (2026-07-19 10차) **주요지표 리스트 명칭 정리**: "누적평균" → "평균"(전 센터 공통, `renderTrendList()`). "전체재직인원(TO대비)" → "전체재직인원"(KB손보부천·KB손보정비). "총합계/고지의무/통지의무/목적물소멸 접수건" → "총/고지/통지/목적물 접수건"(KB손보정비, `CENTER_KPI_DEFS.kbjeongbi`의 `section` 값 변경). "실적·인입호"(제휴상담/장기손사 두 항목) → "제휴 인입호"/"손사 인입호"(KB손보부천). "TO(합계)/총재직인원(합계)/AS재직인원(합계)/성수기재직인원(합계)/상담사투입인원(합계)"에서 "(합계)" 접미사 제거(LG전자통합). "투입인원(TO대비)" → "상담재직인원"(평택시청).
- ✅ (2026-07-19 10차) **맨 하단 실적데이터 표(일별 뷰, `buildMatrix()`)의 열 이름에서 "근태·"/"실적·" 접두사 제거** — 근태·실적 두 그룹이 이미 시각적으로 구분(별도 열 그룹)돼 있어 접두사가 중복 정보였음. 전 센터 공통 적용(집계 뷰 `renderAggTable()`은 원래부터 접두사가 없었음).
- 검증: 기간토글 클릭 시 카드 숫자가 실제로 바뀌는지 확인 (과거 버그였던 지점). KB손보정비 단월 일별그래프에서 토/일/공휴일 데이터가 빠지는지, 다른 센터/다른 기간토글엔 영향 없는지 확인 필요.

## 6. TO 및 목표값설정
- ✅ 연도별 1~12월 가로 표, 체크박스 일괄반영
- ✅ (2026-07-18) "월별 TO 설정" 표의 "선택" 칸이 체크박스 달린 "전체" 토글로 바뀌어, 클릭 한 번으로 12개월 전체 선택/해제 가능(`toGridToggleAll`).
- ✅ (2026-07-19) "주요지표 목표치 설정" 표도 동일하게 "선택" 칸을 체크박스 달린 "전체" 토글로 변경(`kpiGridToggleAll`, 두 표의 UI 패턴 통일). 기존에 있던 별도의 "월 전체 선택"/"월 전체 해제" 버튼은 제거(체크박스 토글로 대체되어 중복).
- ✅ 핵심지표 월별 목표치 오버라이드 (`center_kpi_monthly_targets`)
- ✅ (2026-07-12) LG전자AS/성수기의 통화시간(`통화시간_INOUT_초`) 등 duration 지표는 목표값을 `4:00:00` 형식(시:분:초)으로 입력 가능 (`DURATION_METRIC_KEYS`, `parseKpiTargetInput()`/`formatKpiTargetDisplay()`). 저장은 항상 초 단위 숫자, 화면 표시만 H:MM:SS로 변환. 기본목표값/월별그리드/일괄반영 입력창 모두 적용.
- ✅ 센터별로 "지표 선택" 드롭다운에서 그 센터의 실제 실적항목(성과 데이터 필드) 중 하나를 골라 핵심지표로 등록하고, 기본 목표값 + 월별 목표값(오버라이드)을 각각 설정 가능(`saveKpiSetting`, `saveKpiGrid`). 등록된 지표가 하나 이상 있으면 그 지표들(전체, 등록 순서대로)이 워크스페이스 전체현황 카드의 핵심지표로 그대로 표시됨(`computeCenterHeadline`) — 없으면 센터별 기본 지표세트(`DEFAULT_HEADLINE_METRICS`)로 대체.
- ✅ (2026-07-17 6차 버그수정) **"지표 선택" 드롭다운이 전역 `rowSchema`(데이터입력 화면에서 마지막으로 연 센터 기준)를 참조하던 버그**를 수정 — 설정화면을 다른 센터에서 바로 열거나 데이터입력을 안 거치고 열면 드롭다운이 비어있거나 다른 센터 항목을 보여줬음(LG전자통합은 이 전역변수를 아예 안 채워서 항상 비어 있었음). `loadSettingsMetricSchema()`가 설정화면을 열 때마다 해당 센터 스키마를 새로 조회하도록 수정(LG전자통합은 서버 스키마가 없어 `LGE_TOTAL_PERF_METRICS` 고정 목록 사용). 사실상 "센터별 실적항목을 핵심지표로 선택하는 기능"은 기존에도 있었지만 이 버그 때문에 못 쓰던 상태였음.
- ✅ (2026-07-17 6차) 월별 목표치 표에 "월 전체 선택"/"월 전체 해제" 버튼 추가(`kpiGridToggleAll`). "일괄 반영" 버튼을 누르면 체크된 월에 값을 채우는 것에 그치지 않고 곧바로 저장까지 진행하도록 변경(`applyKpiGridBulk`가 `saveKpiGrid`를 이어서 호출) — 예전엔 값만 채워놓고 "월별 목표치 전체 저장"을 따로 안 누르면 화면 이동 시 그대로 사라졌음.

## 7. 업로드 자료함
- ✅ 데이터입력 시 첨부한 원본 파일 자동 보관 (Storage 버킷 `uploaded-files`)
- ✅ 센터별 필터/파일명 검색/다운로드(서명URL)/개별삭제/일괄삭제
- ✅ (2026-07-13 버그수정) 파일을 선택/추출만 해도 즉시 자료함에 올라가던 문제 수정. 이제 **실제 저장이 성공한 시점에만** 업로드된다(`pendingPerfArchiveFile`/`pendingAttArchiveFile`). 추출만 하고 저장하지 않으면 자료함에 남지 않음.
- ✅ (2026-07-13 4차) 평택시청 일일업무보고(HWPX)는 원래 자료함 업로드 기능 자체가 없었는데, 다른 센터와 동일하게 "전체저장"(`saveEverything`) 성공 시에만 반영되도록 신규 추가.
- 검증: 파일을 추출만 하고 저장하지 않았을 때 자료함에 안 올라가는지, 저장 성공 후에는 올라가는지, 다운로드가 원본과 동일한지 확인. 평택은 일자별실적/업무유형별 인입호 중 한쪽만 저장됐을 때도 자료함에 반영되는지 별도 확인 필요.

## 8. 센터별 업로드 신호등 + 이메일 알림
- ✅ 사이드바 센터명 옆 신호등: 🟢 3일 이내 / 🟠 3일째 미업로드 / 🔴 7일째 미업로드
- ✅ 담당자 다중 등록(센터당 여러 명), 발송대상 on/off
- ✅ 주의(4일째)/경고(8일째) 메일 제목·본문 편집(치환자: `{center_name}`,`{days}`,`{site_link}`), 발송정지/발송시각/반복주기 설정
- ✅ (2026-07-13) "⚡ 즉시 발송" 버튼 추가 — 발송시각/반복주기/중복방지 조건을 모두 건너뛰고 지금 조건(며칠째 미업로드)에 맞는 센터에 바로 발송(`send-notification-now`). `check-and-notify`(매시 크론)와 핵심 로직(`runNotificationCheck`)을 공유해 이중 유지보수 부담 없음. 발송 로그에 즉시발송 여부 표시(`notification_log.is_manual`, `schema_addendum_10`).
- ✅ (2026-07-13 8차) 즉시발송 결과를 센터별 표(성공/실패 + **실패 사유**)로 표시. `sendNotificationEmail()`이 실패 이유(시크릿 미설정/SendGrid HTTP 에러/네트워크 오류)를 구분해서 반환하고 `notification_log.send_ok`/`send_error`에도 기록됨(`schema_addendum_10`). "대상 N건"은 담당자 등록 건수가 아니라 **지금 주의/경고 조건에 걸린 센터 수**라는 점 문서화.
- ✅ (2026-07-13 9차) 즉시발송을 "이 센터만"/"전체 센터"로 분리(`send-notification-now`가 `center_code` 선택적 파라미터 지원). SendGrid 관련해서는 `SENDGRID_API_KEY`는 이미 등록되어 있음을 확인 — 남은 의심 지점은 `SENDGRID_FROM_EMAIL` 미등록으로 인한 발신자 미인증.
- 🟡 발송 로직(SendGrid) — **SENDGRID_API_KEY는 등록 확인됨.** `SENDGRID_FROM_EMAIL` 등록 여부와 SendGrid 발신자 인증 상태는 아직 미확인, 실제 발송 테스트 필요.
- 검증: `notification_log` 테이블에 실제 발송 기록이 쌓이는지 확인 필요

## 9. 스마트업로드 (관리자 전용)
- ✅ 파일 하나 넣으면 센터+실적/근태 유형을 내용/파일명으로 자동 판별
- ✅ LG전자AS/성수기처럼 파일 구조가 같아 구분 안 되는 경우 후보 드롭다운 제공 + 파일명 힌트(예: "성수기", "AS센터")로 우선순위 자동 상향
- ✅ 확인 후 해당 센터 화면으로 자동 이동 + 같은 파일로 추출 자동 실행

## 10. Google Drive 완전자동 연동
- 🟡 센터별 Drive 폴더 등록 → 15분마다 폴링 → 신규 파일 자동 다운로드+분석+반영
- ✅ LG전자AS/성수기: 실적+근태 완전자동 (서버측 파싱 로직 포팅 완료)
- ⛔ KB손보부천/정비/평택시청: 원본 자동 보관까지만 지원 (서버측 파싱 미포팅 — 실제 파일로 검증 후 추가 예정)
- **필요 시크릿**: `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` (미등록 시 폴링이 에러를 `gdrive_processed_files`에 남김)
- 검증: 실제 Drive 폴더에 파일 업로드 → 15분 후 `list-gdrive-log`에 success 기록 확인 필요 (아직 실사용 미검증)

## 11. 사이드바 UI
- ✅ 다크톤(KT 스타일), 섹션 구분("메뉴"/"센터 (N)"/"관리")
- ✅ 계정 메뉴 드롭다운(관리자화면 비번변경/센터 비번변경/백업복원/잠그기)
- ✅ 커스터마이징 링크(Claude 대화/GitHub/Supabase/Vercel + Anon Key) — 관리자화면 비밀번호로 잠금
- ✅ (2026-07-12) 상단 고정 탭 제거, 선택된 센터 아래 서브메뉴(대시보드/데이터입력/이슈및히스토리/TO및목표값설정) 방식으로 전환 (섹션 1 참고)
- ✅ (2026-07-12) `docs/DESIGN.md` 기준 전면 다크 리스킨(배경/텍스트/보더/라운드/버튼 pill화) 적용, 기능·DOM·핸들러는 무변경. 센터별 브랜드/차트 계열색은 유지.
- ✅ (2026-07-22) **로그인 유지 기간을 브라우저 재시작 이후에도 유지되도록 변경**: `restoreSession()`/`persistSession()`이 `sessionStorage`(탭을 닫으면 즉시 소멸) 대신 `localStorage`(직접 "잠그기"를 누르거나 브라우저 데이터를 지우기 전까지 유지)를 사용하도록 변경. 매 접속 시 캐시된 비밀번호를 서버에 실제로 재검증하는 로직(`tryWorkspaceLogin`)은 그대로라 보안 모델 자체는 바뀌지 않고, "얼마나 오래 기억하느냐"만 늘어남 — 직전에 제거한 Origin 헤더 기반 자동인증(누구나 URL만 알면 우회 가능)과는 달리, 이 값을 읽을 수 있는 건 그 브라우저/기기에 실제로 접근 가능한 사람으로 한정됨. "🔒 잠그기"를 누르면 즉시 지워짐.

## 12. 이슈 및 히스토리 (전 센터 공통)
- ✅ 센터별 이슈/히스토리 등록(날짜/제목/내용), 검색(제목·내용), 수정/삭제
- ✅ (2026-07-12) 목록은 제목+날짜만 표시하고, 클릭(펼치기)하면 상세 내용이 아코디언(`<details>`)으로 펼쳐지는 방식으로 변경 (`renderIssueList()`, 전 센터 공통 함수라 자동으로 5개 센터 모두 적용됨)
- 🟡 (2026-07-13) "🤖 AI 요약" 버튼 추가 — 최근 이슈 최대 50건을 반복패턴/미해결추정/특이사항 관점으로 AI가 요약 (`summarizeIssuesWithAI()`). **프론트엔드는 완성됐지만 백엔드 액션(`ai-summarize-issues`)이 아직 미배포**라 실제로는 동작하지 않음 — 섹션13 참고.
- 검증: 이슈 등록 후 목록에서 접힌 상태(제목·날짜만) 확인 → 클릭 시 내용 펼쳐짐 → 수정/삭제 버튼이 펼치기 토글과 겹치지 않고 정상 동작하는지 확인

## 13. AI 보조기능 (🟡 코드는 완성, 배포/시크릿 등록만 남음)
- 🟡 **엑셀 양식 변경 자동대응(KB손보부천)**: 엑셀 자동추출이 "일자별 데이터를 찾지 못했습니다"로 실패하면 "🤖 AI로 양식 분석하기" 버튼이 뜬다. 클릭하면 헤더 텍스트(시트별 최대 12행)와 기존 필드정의(기대 키워드)를 AI에 보내 새 헤더 키워드 매핑을 제안받고, 검토 후 "이 매핑 저장하고 다시 추출"을 누르면 센터별로 저장(`center_xlsx_field_override`)되어 **다음부터는 AI 호출 없이 자동 적용**됨(`getEffectiveXlsxFields()`가 기본 정의 위에 override를 병합). 데이터입력 화면 진입 시 저장된 override를 자동으로 불러옴(`loadXlsxFieldOverride`).
- 🟡 **이슈 히스토리 AI 요약**: 섹션12 참고.
- **Provider 정책**: 서강MOT API **단일** Provider만 사용, 모델은 GPT5.5로 고정(선택 기능 없음). 다른 Provider로의 자동 failover는 없음 — 서강MOT API가 실패하면(크레딧 소진 등) `renderAiUnavailableNotice()`가 "AI 기능을 지금 사용할 수 없습니다" 배너를 띄우고, 기존 수동 붙여넣기·자동추출·저장 흐름은 AI와 완전히 무관하게 그대로 동작한다는 점을 명시적으로 안내한다.
- ✅ (2026-07-13) 사용자가 실제 `index.ts`를 제공해줘서 **AI 액션 3개(`ai-suggest-xlsx-mapping`/`save·get-xlsx-field-override`/`ai-summarize-issues`) 전부 실제 index.ts에 병합 완료**. 더 이상 "병합용 스니펫" 상태가 아니라 바로 배포 가능한 완전한 파일 상태.
- **남은 것은 배포뿐**:
  1. `schema_addendum_9_ai_provider.sql` 실행 (신규 테이블 2개: `center_xlsx_field_override`, `ai_call_log`)
  2. Function Secrets: `MOT_GATEWAY_URL`/`MOT_GATEWAY_KEY` **이미 등록되어 있음 확인됨**(다른 kkangbi 프로젝트와 값 공유) — 추가 등록 불필요
  3. 서강MOT Gateway의 실제 엔드포인트/요청·응답 스펙이 OpenAI 호환(chat/completions)과 다르면 `callSogangMOT()`만 조정
  4. `supabase functions deploy center-report-upload`로 재배포 후 두 기능 실제 동작 검증(정상 케이스 + 크레딧 소진 등 실패 케이스 모두)

## 14. 초기 로딩 속도 개선 (2026-07-13)
- ✅ Chart.js/XLSX 라이브러리 `<script>` 태그에 `defer` 추가 — 두 라이브러리 다운로드가 끝날 때까지 HTML 파싱/첫 렌더가 막히지 않도록 함. 두 라이브러리 사용은 전부 함수 내부(파일선택/차트렌더 시점)에서만 일어나 안전함(모듈 최상단에서 즉시 호출하는 코드 없음을 확인).
- ✅ 자주 안 바뀌는 조회성 API(스키마/설정값 등 6종)에 짧은 캐시(`jsonCached`, `private, max-age=20`) 적용 — 센터 전환·탭 이동을 반복할 때 매번 새로 왕복하지 않고 재사용. 실적/근태처럼 실시간성이 중요한 데이터는 기존처럼 항상 최신(`no-store`) 유지.
- ✅ (기존부터 있던 최적화, 참고) `init()`에서 설정데이터/최근업로드/실적조회를 `Promise.all`로 병렬 요청 — 이미 순차 호출은 아니었음.
- 검증: 재배포 후 개발자도구 Network 탭에서 초기 진입 시 라이브러리 다운로드가 렌더링을 막지 않는지, 센터 반복 전환 시 캐시된 조회 API가 체감상 더 빠르게 응답하는지 확인.

## 15. LG전자통합 (신규 센터, 2026-07-16)
- ✅ 기존 LG전자AS(`lge`)·LG전자성수기(`lge_seongsu`)는 그대로 유지하고, **완전히 독립된 신규 센터**로 `lge_total`(LG전자통합) 추가. 두 원본 센터의 데이터를 자동으로 가져오지 않음(사용자 확인 완료 — 완전히 새로 입력하는 방식).
- ✅ 파일첨부/붙여넣기 대신 **항목을 만들고 숫자만 입력하는 전용 폼**(`renderIntegratedFormEntry`, `INTEGRATED_FORM_CENTERS`)으로 데이터입력 화면이 통째로 바뀜(추출/붙여넣기 카드 자체가 안 보이고 이 폼만 나옴).
- ✅ (2026-07-17 3차, 항목 구조 개편으로 아래 내용으로 대체됨) 입력 항목 순서: 날짜, TO(AS·성수기·합계), 총재직인원(AS·성수기·합계, **자동계산·직접입력 불가**), AS재직인원(관리자·상담사·합계), 성수기재직인원(관리자·상담사·합계), 상담사투입인원(AS·성수기·합계), T-NPS, 생산성_INOUT, 생산성_IN, 통화시간(H:MM:SS 입력, 초 단위 저장 — 기존 LG센터와 동일 방식).
  - 기존 "관리자재직인원(AS/성수기/합계)"·"상담사재직인원(AS/성수기/합계)" 2개 항목을 "AS재직인원(관리자/상담사/합계)"·"성수기재직인원(관리자/상담사/합계)" 2개 항목으로 축(axis)을 바꿈. 저장 키도 `관리자재직인원_AS` 등에서 `AS재직인원_관리자` 등으로 **완전히 교체**됨 — 개편 이전에 저장된 날짜는 관리자/상담사 분해값 없이 다시 입력해야 함(사용자 확인 후 진행).
  - "총재직인원"은 더 이상 직접 입력하지 않고, AS 값은 AS재직인원의 합계를, 성수기 값은 성수기재직인원의 합계를 그대로 가져와 자동 계산. `CENTER_KPI_DEFS.lge_total`의 대시보드 표시 항목도 "AS재직인원(합계)"/"성수기재직인원(합계)"로 함께 변경.
  - (2026-07-17 5차) **총재직인원과 각 항목의 "합계" 칸은 화면 표에 아예 표시하지 않음** — 사용자가 직접 입력하는 세부값(TO의 AS/성수기, AS재직인원의 관리자/상담사, 성수기재직인원의 관리자/상담사, 상담사투입인원의 AS/성수기)만 보이고, 합계·총재직인원은 "전체 저장" 시 서버로 보낼 값을 계산할 때만 내부적으로 자동 계산됨(`LGE_TOTAL_VISIBLE_ATT_METRICS`, `saveLgeTotalRows`). 화면이 21개 열에서 14개 열로 단순해짐.
- ✅ TO·AS재직인원·성수기재직인원·상담사투입인원 4개 항목은 **직전 저장된 날짜의 값을 자동으로 기본값으로 채움**(연쇄 이월). 세부값은 언제든 직접 수정 가능하며 합계·총재직인원은 저장 시 자동 반영. T-NPS·생산성·통화시간은 이월 없이 매일 새로 입력.
- ✅ (2026-07-17 3차, 5차 개편) **월평균 실적 입력**: T-NPS/생산성_INOUT/생산성_IN/통화시간 4개의 "이번 달 월평균" 입력칸(선택 입력)을 시작일 위에 배치. (2026-07-17 5차) 처음엔 "시작일이 속한 달"로 자동 결정했으나, `<input type="month">`로 **"대상 월"을 직접 선택**하도록 변경(`lgeTotalAvgMonth`, `lgeTotalAvgMonthDate()`) — 날짜별 실적표의 시작일과 완전히 무관하게 원하는 달을 골라 그 달의 월평균을 입력할 수 있음. 값을 넣고 저장하면 날짜별 데이터와는 별개로 **선택한 달의 1일 데이터**에 `_월평균` 접미사가 붙은 키(예: `TNPS_월평균`)로 저장됨(신규 백엔드 액션·SQL 없이 기존 `manual-entry-bulk` 재사용). "대상 월"을 바꾸거나 화면을 처음 열 때 그 달 1일에 이미 저장된 월평균 값이 있으면 자동으로 불러와 보여줌(`refreshLgeTotalMonthlyAvgInputs`).
- ✅ (2026-07-17 5차) **통화시간 자동 포맷**: 통화시간 입력칸(일자별 실적/일괄입력/월평균 전부)에 숫자만 입력하고 포커스를 벗어나면(blur) 오른쪽부터 초·분·시 순으로 나눠 `H:MM:SS` 형식으로 자동 변환(`formatDurationOnBlur`). 예: `40000` → `4:00:00`, `145` → `0:01:45`. 이미 콜론이 들어간 값은 건드리지 않음.
- ✅ (2026-07-17 3차) **날짜별 제외 체크박스**: 표 맨 왼쪽에 날짜별 "제외" 체크박스 추가(`lgeTotalToggleRowExclude`). 체크하면 그 행이 흐리게 표시되고, "전체 저장" 시 그 날짜는 저장 대상에서 빠짐(DB에 이미 저장된 데이터를 삭제하는 것이 아니라 **이번 저장 요청에서만 제외**하는 용도).
- ✅ 저장은 기존 `manual-entry-bulk` 액션 그대로 재사용(신규 백엔드 액션 없음). "저장된 데이터 조회·수정·삭제" 패널도 그대로 재사용하되, "수정하기"는 이 센터 전용 폼으로 불러오도록 분기 처리(`loadLgeTotalRowForEdit`).
- ✅ (2026-07-19) **대시보드를 LG전자성수기와 동일한 형태로 구성**: `CENTER_TO_INFO`/`CENTER_CHART_CONFIG`/`DEFAULT_HEADLINE_METRICS`에 `lge_total` 항목을 추가해 요약카드(전체 운영요약 + 생산성 그룹카드)·지표추이 미니차트(생산성 IN/OUT 누적막대 + T-NPS 선)·워크스페이스 전체현황 헤드라인 카드까지 전부 다른 LG전자 센터와 동일하게 표시됨. TO는 이 센터만 고정값이 아니라 매일 입력되는 값이라, `CENTER_TO_INFO.dynamicTargetAttKey`로 그 기간에 입력된 TO_합계 평균 대비 재직인원 비율을 계산하도록 `renderSummaryCards()`를 일반화(다른 센터는 기존 고정 TO_TARGET 방식 그대로).
- ✅ (2026-07-19) **월평균 실적을 일별 자동계산이 아니라 수동입력값 기준으로 표시**: T-NPS/생산성_INOUT/생산성_IN/통화시간, 이 4개 지표는 대시보드(요약카드/주요지표 리스트/워크스페이스 헤드라인 카드 전부)에서 일별 데이터 평균이 아니라 데이터입력 화면에서 입력해둔 `{key}_월평균` 저장값을 그대로 사용(`avgMetricValue`, `getLgeTotalMonthlyAvgValue`). 여러 달이 걸친 기간(분기/반기/연초누적)이면 해당 달들의 월평균값을 다시 평균. 파생값인 생산성(OUT)도 월평균 기준 INOUT-IN으로 일관되게 계산. 지표추이 미니차트(일별/주별 등 그래프 자체)는 기존처럼 일별 실측값을 그대로 보여줌 — 바뀐 건 카드/리스트에 찍히는 "요약 숫자"뿐.
- ✅ (2026-07-19 2차) **대시보드 세부 보정 8건**:
  1. 지표추이 그래프 툴팁 "총합계"가 T-NPS(선 그래프)까지 합산하던 버그 수정 — 막대(생산성 IN/OUT)만 합산하도록 수정(`drawMiniCharts` 툴팁 footer, `ti.dataset.type === 'bar'`로 필터링. lge_total뿐 아니라 stacked+line을 함께 쓰는 모든 센터에 공통 적용되는 일반 버그였음).
  2. T-NPS 꺾은선 그래프가 주말(값 없음)에서 끊기던 것을 `spanGaps: true`로 이어지도록 수정(전 센터 라인 데이터셋 공통 적용).
  3. 지표추이 막대그래프의 주축(`yCount`, 좌측) 범위를 100으로 고정(`CENTER_CHART_CONFIG.lge_total.groups[0].barMax`, 신규 옵션 필드 — 다른 센터는 미설정이라 기존처럼 자동 스케일 유지).
  4. 요약카드(생산성 카드)에 "월평균 통화시간"을 `0:00:00` 형식으로 추가 표시(`group.summaryExtraKeys`, 차트에는 그려지지 않고 요약카드에만 표시되는 신규 옵션 필드 — 기존 `lineKeys`를 재사용하면 차트에도 그려지고 스케일이 T-NPS와 안 맞아 별도 필드로 분리함).
  5. 데이터 표(`buildMatrix`/`renderAggTable`)의 `통화시간_INOUT_초`를 초 단위 숫자 대신 `0:00:00` 형식으로 표시(`DURATION_TABLE_KEYS`, 합계/평균 집계값도 동일 적용).
  6. 데이터 표에서 `_월평균` 접미사 항목(TNPS_월평균 등 4개)을 항상 숨김 — 일별 원자료가 아니라 별도 요약값이라 원자료 표에 섞이면 혼란을 줌(`collectKeys`가 `filterVisibleTableKeys`로 필터링).
  7. 데이터 표에서 자동계산으로 인해 값이 항상 같은 중복 항목(`AS재직인원_합계`=`총재직인원_AS`, `성수기재직인원_합계`=`총재직인원_성수기`) 중 `총재직인원_AS`/`총재직인원_성수기`만 남기고 나머지는 숨김(`HIDDEN_DUPLICATE_ATT_KEYS`).
  8. **TO_합계 관련 확인**: "TO_합계 = 총재직인원_AS + 총재직인원_성수기로 적용해달라"는 요청이 있었는데, 코드 확인 결과 그 계산식은 이미 `총재직인원_합계`가 정확히 그렇게 계산되고 있었음(`saveLgeTotalRows`, `총재직인원_AS`=`AS재직인원_합계`, `총재직인원_성수기`=`성수기재직인원_합계`, `총재직인원_합계`=둘의 합). 반면 `TO_합계`는 `TO_AS`+`TO_성수기`(목표 재직인원, 직접 입력값)로 실제 재직인원과는 별개 개념이라 그대로 두면 대시보드 요약카드의 "TO대비 재직인원 비율" 계산(`dynamicTargetAttKey`)이 깨짐 — 용어 혼동으로 판단해 TO_합계는 변경하지 않음. 의도가 다르면 알려주시면 다시 반영.
- ✅ (2026-07-19 3차) **AS 또는 성수기 중 한쪽만 입력해도 합계가 정상 계산되도록 수정**: `saveLgeTotalRows()`가 TO/AS재직인원/성수기재직인원/상담사투입인원의 `_합계`를 "두 부분(AS·성수기 또는 관리자·상담사) 모두 입력됐을 때만" 계산해서, 하루는 AS만 입력하고 성수기는 안 넣은 경우(또는 그 반대) `TO_합계`/`상담사투입인원_합계` 등이 통째로 비어 보이는 문제가 있었음(파생 항목인 `총재직인원_합계`도 같은 이유로 연쇄적으로 비어 보였음). 이제 둘 중 하나만 입력돼도 입력 안 된 쪽을 0으로 취급해 합계를 계산·저장하도록 수정 — 데이터 표의 `근태·TO_합계`/`근태·총재직인원_합계`/`근태·상담사투입인원_합계`가 부분 입력된 날에도 값을 보여줌.
- ✅ (2026-07-19 4차) **위 저장 시점 수정과 별개로, TO_합계/총재직인원_합계/상담사투입인원_합계를 "저장된 값을 그대로 믿지 않고 조회할 때마다 AS+성수기를 실시간으로 더해서" 보여주도록 한 단계 더 보강**(`LGE_TOTAL_LIVE_SUM_KEYS`, `lgeTotalLiveAttSum()`, `extractNum()`에서 가로챔). 저장 시점 수정 이후에 새로 저장되는 데이터는 이미 정확하지만, **그 이전에 저장된 과거 데이터**는 여전히 합계가 비어 있거나(한쪽만 입력됐던 날) 오래된 값 그대로 남아있을 수 있어서, 데이터 표(`buildMatrix`/`renderAggTable`)뿐 아니라 요약카드의 "TO 대비 재직인원" 비율(`renderSummaryCards`)·주요지표 트렌드 리스트(`getAvgForDef`)·워크스페이스 전체현황 헤드라인 카드(`computeCenterHeadline`)까지 이 3개 필드를 읽는 모든 화면에서 항상 AS+성수기 실시간 합산값을 보여줌(둘 다 값이 없는 날은 억지로 0을 만들지 않고 그대로 빈칸 유지). `extractNum()` 한 곳만 고쳐서 이를 읽는 모든 화면에 자동 반영되도록 했고, `lge_total`이 아니거나 이 3개 키가 아니면 기존 동작 그대로라 다른 센터/다른 항목은 영향 없음.
- ✅ (2026-07-16 2차, 2026-07-17 UI 개편으로 대체됨 — 아래 참고) 날짜별 체크박스 + "전체 선택/해제" + "선택 날짜에 일괄 적용" 방식은 폐기.
- ✅ (2026-07-17) 표 맨 위 "일괄입력" 행으로 개편(`applyLgeTotalColumn`, `applyLgeTotalAllColumns`). 체크박스로 날짜를 고르던 방식 대신, 항목(AS/성수기/실적)별로 표 맨 위 행에 값을 입력하고 그 항목 칸의 "전체반영" 버튼을 누르면 **해당 항목만** 전체 날짜에 채워짐(합계는 자동 재계산). 여러 항목을 동시에 입력해두고 "⚡ 전체 항목 전체반영" 버튼을 누르면 값이 채워진 항목 전부가 한 번에 전체 날짜에 반영됨. 항상 "표에 보이는 전체 날짜"가 대상이라 더 이상 날짜를 개별 선택할 필요가 없음.
- ✅ (2026-07-19 6차, 8차로 2줄 구성으로 대체됨 — 아래 참고) 요약카드 "생산성" 카드 첫 줄을 "T-NPS, 생산성(IN+OUT), 생산성(IN), 생산성(OUT)" 순서로 한 줄에 표시.
- ✅ (2026-07-19 8차, 9차로 줄 구성 재조정됨 — 아래 참고) 요약카드 "생산성" 카드를 "1줄: T-NPS·통화시간(IN+OUT) / 2줄: 생산성(IN+OUT)·생산성(IN)·생산성(OUT)" 두 줄 구성으로 변경. `summaryRows`(줄 단위 2차원 배열) 신규 도입 — `CENTER_CHART_CONFIG.lge_total.groups[0].summaryRows`에 지정된 줄·순서·항목 그대로 `renderSummaryCards()`가 카드 본문을 재구성함(값 형식: duration 타입은 `0:00:00`, 그 외는 소수 첫째자리). 차트(`drawMiniCharts`)는 기존 `barKeys`/`lineKeys` 구성 그대로라 영향 없고, `summaryRows`를 지정하지 않은 다른 센터는 기존 막대/선 레이아웃 그대로 유지.
- ✅ (2026-07-19 9차) **요약카드 "생산성" 카드 줄 구성을 "1줄: T-NPS·통화시간(IN+OUT)·생산성(IN+OUT) / 2줄: 생산성(IN)·생산성(OUT)"으로 재조정**: `summaryRows` 메커니즘은 8차 그대로 재사용, `CENTER_CHART_CONFIG.lge_total.groups[0].summaryRows`의 줄 구성만 변경(생산성(IN+OUT)을 1줄로 옮기고 2줄은 생산성(IN)·생산성(OUT) 2항목만 남김).
- ✅ (2026-07-21 2차) **데이터입력 화면 월평균 입력 UI 정리**: "이번 달 월평균 실적 입력" 아래 붙어 있던 "(선택 — 값을 넣으면 선택한 달의 1일 데이터에 별도 항목으로 저장됩니다)" 안내 문구 제거. `LGE_TOTAL_PERF_METRICS`의 통화시간 라벨을 "통화시간"→"통화시간(IN+OUT)"으로 변경(일자별 입력 표 헤더·일괄입력 행·월평균 입력칸 전부 이 배열을 공유해서 자동으로 반영됨). 월평균 입력 블록의 통화시간 라벨 옆에는 "(0:00:00 형식, 기본 4:00:00)" 형식 안내를 추가로 표시(입력 형식·placeholder·자동변환(`formatDurationOnBlur`)은 이미 모든 통화시간 입력칸에 적용돼 있던 상태 그대로).
- **SQL 신규**: `schema_addendum_11_lge_total_center.sql` (센터 등록, 기본 비밀번호 000000 — 로그인 후 변경 필요)
- 검증: SQL 실행 후 사이드바에 "LG전자통합"이 뜨는지, 기간 적용 시 전날 값이 잘 이월되는지, 합계 자동계산이 실시간으로 되는지, 저장 후 데이터조회에서 수정이 잘 되는지, 체크박스 일괄적용이 의도한 날짜에만 반영되는지 확인 필요.

## 16. 화면 반응속도 개선 — 캐시 우선 렌더링 (2026-07-16)
- ✅ 센터 전환/새로고침 시, 그 센터를 이전에 본 적이 있으면(세션 메모리 또는 localStorage 캐시) **네트워크 응답을 기다리지 않고 캐시된 데이터로 화면부터 먼저 그림**(`getCachedRows`/`setCachedRows`, `selectCenter`/`init` 수정). 이후 실제 최신 데이터를 받아와서 **캐시와 실제로 다를 때만** 조용히 한 번 더 갱신 — 내용이 같으면 재렌더링을 생략해 불필요한 화면 깜빡임/연산을 줄임(`loadOverviewForCurrent`가 변경 여부를 반환하도록 수정).
- 처음 방문하는 센터(캐시 없음)는 기존과 동일하게 데이터를 받아온 뒤 렌더링(동작 변화 없음, 첫 방문에서는 체감 개선 없음 — 두 번째 방문부터 체감됨).
- 검증: 같은 센터를 반복해서 왔다갔다 할 때 클릭 즉시 화면이 바뀌는지(로딩 지연 감소), 실제 데이터 변경이 없을 때 화면이 불필요하게 깜빡이지 않는지 확인.

## 다음 세션에서 우선 확인할 것
1. SendGrid 실제 발송 테스트 (섹션 8)
2. Google Drive 완전자동 실사용 테스트 (섹션 10)
3. KB손보부천/정비/평택시청 서버측 파서 포팅 (실제 파일 필요)
4. AI 보조기능(섹션 13) 백엔드 배포 및 실동작 검증 — 현재 프론트엔드만 완성된 상태
